# Secrets / credentials
.env
.env.*
*.env
secrets.*
**/config.local.json
.opencode/config.json

# [007-MCP] rutas MCP locales (nunca versionar)
.env.mcp

# Local opencode runtime
.opencode/node_modules/

# OS / editor
Thumbs.db
.DS_Store
.vscode/
.idea/

# Clones externos (código de terceros, no propio — se regeneran desde scripts, no se versionan)
proyect_ext/tokenslayer/

# Respaldos de huérfanos por revisar (no versionar — pueden contener secrets)
revisar_manualmente/
.github/context-mode/


# Grafos Graphify (artefactos regenerables — nunca versionar; mitiga subir .env/claves/datos no requeridos)
graphify-out/

