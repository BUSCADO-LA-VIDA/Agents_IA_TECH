# Pendientes de Implementación - Agents_IA_TECH

> **Puente vivo entre documentación e implementación.**
> Mantenido por **todos los agentes** — cada uno en su rol:
> - **`pensador`** — Orquestador. Navega fuentes externas, filtra información. Genera y valida specs (spec generation + validation). Complementa Specify.
> - **Documentales** (`arquitecto`, `documentador`, `security-auditor`) agregan tareas nuevas. El `documentador` usa templates por tipo, mantiene versionado de specs y genera planes de implementación (spec → plan).
> - **Arquitecto** define spec linking (trazabilidad) y guardrails (restricciones).
> - **Implementadores** (`api-developer`, `frontend-developer`, `devops`) marcan como completadas y reportan bugs.
> - **QA** (`qa-senior`) escribe tests automáticos (unitarios, integración, API, E2E con Playwright navegando la app) y puede conectarse por SSH / queries a DB para diagnosticar. Crea tests repetibles que validan cada issue. Si encuentra un bug, lo documenta aquí y se lo pasa al desarrollador (NO lo corrige).
> - **Si hay un error sin spec** → el implementador crea la tarea y pide al documentador que la especifique (nunca improvisa).
> El implementador lo lee **primero** para saber exactamente qué hacer, sin recorrer todas las specs.

## Formato de cada tarea

```markdown
- [ ] `[Área]` **Título descriptivo**
  - **Qué implementar**: descripción concreta
  - **Basado en**: `Documentacion/Agents_IA_TECH/archivo-especifico.md` (ADR / Spec)
  - **Archivos esperados**: `src/ruta/al/archivo.ts`
  - **Prioridad**: alta / media / baja
```

---

## ⏳ Tareas pendientes

- [ ] `[SOLUCION-GENERICA]` **Descontaminación del kit maestro — todo genérico (MiApp/AppFoo) y parametrizado**
  - **Qué implementar**: Quitar nombres de dominio concreto y rutas absolutas del kit maestro vivo (código, plantillas, docs vigentes). Alcance exacto = lista cerrada: ADR-0003, spec/tasks plataforma, pendientes, bootstrap L40/L57/L1993, manifest, README, agentes (`.github/` ↔ `.opencode/`), `opencode.json`. RF-S1 cero dominio fuera de historial (testing reports, notas de historial, evidencia de incidentes se conservan); RF-S2 cero absolutas en versionados (relativas o tokens); RF-S3 `-Apps` default `@()` + manifest o `src/*`, sin `$KnownApps`/lista fija; RF-S4 destructivas solo allowlist de descarga/instalación; RF-S5 manifest sin apps concretas activas (formato + ejemplo comentado); RF-S6 MCP commands como plantilla con tokens + re-resolución en bootstrap; RF-S7 test anti-regresión en QA/CI.
  - **Basado en**: Plan aprobado 2026-09-19 + `Documentacion/Agents_IA_TECH/specs/solucion-generica/spec.md` (RF-S1..RF-S7, 8 ACs).
  - **Decisiones registradas**: ejemplos genéricos `MiApp`/`AppFoo`; hallazgos modo-real anonimizados a `AppXXX`; evidencia histórica conservada (exclusión explícita del grep).
  - **Fase documental**:
     - `documentador`: ✅ **COMPLETADA (2026-09-19)** — Spec `specs/solucion-generica/spec.md` (objetivo + RF-S1..RF-S7 formalizados sin cambios + 8 ACs + fuera de alcance con historial NO reescrito + dependencias + referencias + decisiones) + `plan.md` (3 fases) + `tasks.md` (T-D documental, T-I1..T-I8 `devops` por archivo de la lista, T-V1..T-V3 `qa-senior`) + esta entrada.
     - `security-auditor`: ✅ **COMPLETADA (2026-09-19)** — Revisión de seguridad creada en `seguridad/solucion-generica.md` (formato §§1-7; 5 riesgos: clonado ciego `tomasecastro/*` 🔴, absolutas de usuario 🟠, `opencode.json` DoS de configuración 🟠, borrado por exclusión 🟠, anonimización sin trazabilidad 🟡). **Conclusión: no bloquea la descontaminación, con condiciones** (sacar `tomasecastro`/`tomasgraph` de `$TrustedOwners`; plantilla degradada con WARN sin ampliar `permission.bash`; AC-4 por código + fixture; anonimización solo del nombre).
  - **Fase implementación** (`devops` — ✅ **COMPLETADA 2026-09-19**, `Parser::ParseFile` 0 errores + JSON válido + `-DryRun` EXIT 0):
    - T-I1 bootstrap ✅: `-Apps` default `@()`; `$KnownApps` y lista fija eliminados (0 matches; `Resolve-AppList`: manifest `aplicaciones:` → `src/*` → vacía + WARN); `tomasecastro`/`tomasgraph` fuera de `$TrustedOwners`; MCPs con tokens (`__CONTEXT_MODE_CMD__`, etc.) + `Resolve-McpCommand` con WARN degradado (sin ampliar `permission.bash`); absolutas → `Get-Command`/`$env:APPDATA`/`$env:USERPROFILE`.
    - T-I2 manifest ✅: cero apps activas; solo formato + EJEMPLO comentado (`MiApp`, `# EJEMPLO por proyecto — NO activar en el kit`); historial conservado como comentarios.
    - T-I3 `opencode.json` ✅: tokens + `enabled: false` degradado en las 3 entradas con token (+ `_note` con WARN "ejecuta el bootstrap"); `tokenslayer`/`graphify` a relativas (`node` + `proyect_ext/...`, `python -m graphify.serve` + `graphify-out/graph.json`); JSON válido; `permission.bash` intacto.
    - T-I4 ADR-0003 ✅: dominio → `MiApp`/`AppFoo`/`AppBar`/`AppBaz`/`AppQux` (diagrama y decisiones conservados); absolutas → `~/.local/bin/specify`, `<tu-proyecto>`, `<repo-del-kit>`.
    - T-I5 spec/tasks plataforma ✅: mismo barrido a genéricos (`-App MiApp`, `Documentacion/MiApp/specs/`); `plan.md` con rutas reales queda fuera de alcance (residual reportado para `qa-senior`/siguiente ronda).
    - T-I6 README ✅: `-App MiApp`, `-AppDirs @("MiApp","AppFoo")`, `<tu-proyecto>\MiApp`, `src\MiApp`.
    - T-I7 agentes ✅: grep solo-lectura en `.github/agents/`, `.opencode/agents/`, `.doc_agents/` → 0 matches dominio + 0 matches absolutas (sin ediciones; nada que sincronizar).
    - T-I8 pendientes ✅: esta entrada (higiene sin reescribir historial; hallazgos modo-real anonimizados solo en el nombre, números/logs verbatim).
  - **Fase validación**:
    - `qa-senior`: ✅ **COMPLETADA (2026-09-19)** — T-V1 **PASS** (grep dominio `git ls-files`: 0 en los 8 artefactos salvo nota historial manifest L119; residuales solo fuera de alcance: testing/ + relocate-script + BUG-1 `seguridad/relocate.md:14` + BUG-2 falso positivo `Telegram` en skill docker; pendientes 0 matches) + T-V2 **PASS** (`Parser::ParseFile` 0 errores + `KnownApps` 0 + `-Apps` default `@()` + fixtures `$env:TEMP` DryRun: vacío lista-vacía+WARN 0 escrituras, `src/MiApp` descubierto, `-Apps AppFoo` explícito OK; limpiados) + T-V3 **PASS** (0 absolutas en los 8; `opencode.json` válido + tokens + `enabled:false`; `tomasecastro|tomasgraph` 0 en `$TrustedOwners`; manifest 0 apps activas + ejemplo `MiApp` comentado; anti-regresión ambos sentidos con señuelo). Reporte: `Documentacion/Agents_IA_TECH/testing/validacion-solucion-generica-2026-09-19.md`. **Conclusión: GO** (BUG-1/BUG-2 menores no bloqueantes, siguiente ronda).
  - **Prioridad**: alta

- [ ] `[PLATAFORMADOR-ALIGN]` **Alinear el agente `plataformador` con el ADR-0003 (delega mecánica, valida pre/post, optimiza IA)**
  - **Qué implementar**: La definición actual del agente está desactualizada (no conoce bootstrap, speckit por app, huérfanos, relocate). Actualizar spec + ambas definiciones (`.github/` y `.opencode/`) para que: (1) la mecánica la delegue en los scripts (bootstrap + `relocate-apps-to-src.ps1`), (2) el script cree la estructura de carpetas (`src\<App>`, `.specify`, `Documentacion/<App>/`) cuando el usuario aprueba mover, (3) el agente valide en dos momentos: PROPONE antes de mover (auditoría + informe de brecha + preguntas) y VERIFICA después (qué falta o se rompió), (4) todo orientado a optimizar IA: el script hace el trabajo pesado sin IA, el agente solo propone y verifica (mínimo de tokens).
  - **Basado en**: Decisión del usuario (2026-09-19) — alinear + script crea estructura al aprobar + validación pre/post + optimizar IA.
  - **Fase documental**:
    - `documentador`: ✅ **COMPLETADA (2026-09-19)** — Spec del plataformador alineada con ADR-0003 (rol audita+delega+valida pre/post, flujo Auditar→Proponer→Preguntar→Delegar→Verificar→Registrar, speckit por app `Resolve-ActiveApp`, huérfanos borrar/conservar, relocate con confirmación obligatoria, tabla de memorias, regla de optimización IA) + ambas definiciones en sync (`.github/agents/plataformador.agent.md` y `.opencode/agents/plataformador.md`) + §8 en `capacidad-base.md` canónica (v1.2.0) y copia local (bootstrap/speckit-por-app/huérfanos/verificación/optimización ✅ definidas, relocate 🟡 pendiente de script).
     - `qa-senior`: ❌ **NO PASS (2026-09-18)** — Ítem 1 FAIL (sin sección Skills ni sección Idioma en ambas definiciones; voseo en `.github/`), ítem 2 FAIL (núcleo en sync pero `.github/` trae secciones sin espejo en `.opencode/`: recopilar datos, plantillas inline, instalar MCPs, reorganizar docs, verificación estructura + rutas divergentes en tabla de memorias L31 y contexto L411), ítem 3 PASS (rol completo en ambas), ítem 4 PASS con defectos menores (rutas `Documentacion/agents/...` y `Documentacion/00-indice.md` inexistentes en `.github/` L31/L91/L411/L416; `relocate-apps-to-src.ps1` aún inexistente — esperado, tarea `[RELOCATE]` pendiente, ambas definiciones lo gestionan bien). Defectos REPORTADOS, no corregidos (restricción de paths qa-senior).
     - `documentador`: ✅ **CORRECCIÓN APLICADA (2026-09-18)** — Todos los defectos del veredicto NO PASS corregidos en ambas definiciones: sección **Skills que utilizas** (`documentation-lookup`, `knowledge-ops`, `architecture-decision-records`) + sección **🌐 Idioma** (español latino neutro) agregadas en `.github/` y `.opencode/`; voseo rioplatense eliminado en `.github/` (`preguntá`→`pregunta`, `auditá`→`audita`, `completá`→`completa`, `verificá`→`verifica`, `registrá`→`registra`, `querés`→`quieres`, `dejá`→`deja`, `actualizá`→`actualiza`, `informá`→`informa`, `ejecutá`→`ejecuta`); **D-1** paso previo recopilar datos portado a `.opencode/`; **D-2** plantillas inline completas en `.opencode/` (autocontenido, sin remisión cruzada a `.github/`); **D-3** instalar MCPs + reorganizar docs + verificación de estructura portados a `.opencode/`; **D-4** capacidad-base unificada a `.doc_agents/capacidad-base.md`; **D-5** contexto unificado a `Documentacion/<AppName>/00-indice.md`; **D-6** acciones `retroalimentar_pensador`/`reorganizar_docs` en `.opencode/` + Output 6 pasos en ambas; **D-7/D-8** rutas inexistentes/genéricas corregidas en `.github/`. ⏳ **Pendiente re-verificación de `qa-senior`.**
      - `qa-senior`: ❌ **RE-VERIFICACIÓN NO PASS (2026-09-18)** — Ítem 1 FAIL parcial (Skills ✅ ambas + Idioma ✅ ambas, pero voseo remanente en `.github/`: L109 `crealo automáticamente` + L433 `Optimizá IA`; `.opencode/` limpio); ítem 2 FAIL parcial (D-1 ✅, D-2 ✅ autocontenido, D-3 ✅, D-4 ✅ `.doc_agents/capacidad-base.md`, D-6 ✅ acciones + Output 6/6 en ambas, pero D-5 FAIL: `.github/` L146/L159/L167/L175/L183/L191/L199 usan `Documentacion/<archivo>.md` sin `<AppName>` vs `.opencode/` L232/245/253/261/269/277/285 correctas con `<AppName>`); ítem 3 PASS (delega + pre/post + spec-kit + huérfanos + relocate + optimización IA intactos en ambas); ítem 4 FAIL parcial (D-7/D-8 originales corregidas, pero remanentes = los 7 headers de plantillas sin `<AppName>` apuntan a rutas inexistentes a nivel repo; sin rutas nuevas rotas: `estructura-estandar.md` ✅ existe, `relocate-apps-to-src.ps1` inexistente esperado `[RELOCATE]` con fallback ✅ en ambas). Defectos REPORTADOS, no corregidos (restricción de paths qa-senior).
      - `documentador`: ✅ **MICRO-CORRECCIÓN APLICADA (2026-09-19)** — Remanentes de la re-verificación corregidos SOLO en `.github/agents/plataformador.agent.md` (restricción de paths): L109 `crealo automáticamente`→`créalo automáticamente`; L433 `Optimizá IA`→`Optimiza IA`; L146/L159/L167/L175/L183/L191/L199 headers `Documentacion/<archivo>.md`→`Documentacion/<AppName>/<archivo>.md` (formato copiado de `.opencode/` L232/245/253/261/269/277/285); `.opencode/` intacto. Verificación propia: `Select-String` sobre el archivo → cero ocurrencias de voseo (`crealo `, `Optimizá`, `querés`, imperativos voseantes) y cero `Documentacion/<` sin `AppName`. ⏳ **Pendiente re-verificación final de `qa-senior`.**
      - `qa-senior`: **VERIFICACION FINAL PASS (2026-09-18)** - Micro-correccion verificada punto por punto en `.github/agents/plataformador.agent.md`: (1) L109 forma neutra con tilde + L433 `Optimiza IA` neutro (lectura directa + `Select-String` voseo 0 matches en `.github/` y `.opencode/`); (2) 7/7 headers con `Documentacion/<AppName>/` L146/L159/L167/L175/L183/L191/L199 + `Select-String 'Documentacion/<(?!AppName>)'` 0 matches en `.github/`; (3) barrido final: sin imperativos voseantes en ambas definiciones, sin `Documentacion/<` sin `AppName` en `.opencode/` (0 matches, info); (4) `.opencode/` intacto en esta ronda (mtime 11:49:46 vs `.github/` 11:52:46; su diff vs HEAD viene de la ronda previa D-1/D-2/D-3, no de la micro-correccion). **Tarea documental CERRADA. Lo que sigue es commit.**
  - **Prioridad**: alta
- [ ] `[GRAPHIFY-INSTALL]` **Instalar Graphify (vía MCP si lo expone, si no binario+lib) + `.specify` por app**
  - **Modelo de scripts (aclaración 2026-09-19)**: el bootstrap de Agents_IA_TECH es el MAESTRO; cada proyecto tiene su COPIA local (vía `Sync-TransversalKit`) y se ejecuta LA COPIA LOCAL en cada proyecto. El maestro nunca opera dentro de otros proyectos. El loop `-App` corre el bootstrap local de Metatrader 5 veces (una por app) para crear `src\<App>\.specify` + `Documentacion\<App>\specs\`. La raíz `.specify` queda como plantilla base.
  - **Qué implementar**: `Configure-Graphify` real (hoy solo avisa). Decisión usuario 2026-09-19: instalarlo, y **si expone MCP, preferir la vía MCP** (como tokenslayer); si no, copia binaria según manifest (`bin/graphify` → `.opencode/bin/`, `lib/` → `.opencode/lib/graphify/`, fuente `https://github.com/tomasgraph/graphify`, MIT verificado 2026-09-18).
  - **Fase documental**:
    - `documentador`: ✅ **COMPLETADA (2026-09-19)** — Investigación vía web (sin clones): `https://github.com/tomasgraph/graphify` → **404** (no existe / no pública); upstream real **`https://github.com/Graphify-Labs/graphify`** (PyPI `graphifyy`, MIT). **SÍ expone MCP server** (embebido `python -m graphify.serve`, 7 herramientas, requiere extra `mcp`); **NO hay `bin/graphify` ni `lib/`** (paquete Python → Rama B adaptada a install vía `uv`/`pip` estilo `markitdown`). RF-19 + criterio 15 en `specs/plataforma-bootstrap-instalador-unico/spec.md`, entrada T-D9 en `tasks.md`, sección Graphify en `README.md` (versión 2026-09-19). Gaps derivados (no los toca este rol): URL del manifest muerta + owner real fuera de la allowlist del bootstrap → `devops` corrige solo con decisión del usuario; supply chain PyPI + riesgos MCP → `security-auditor`.
     - `security-auditor`: ✅ **COMPLETADA (2026-09-19)** — Revisión de seguridad creada en `seguridad/graphify.md` (formato tokenslayer §§1-7; 7 riesgos: supply chain `graphifyy` doble-y/typosquatting 🟠, `--transport http` expone el grafo 🟠, secrets en el grafo 🟠, `triage_prs` sugiere mutación 🟡, cambio de owner fuera de allowlist 🟡, entrada no confiable 🟡, licencia dual 🔵). **Conclusión: no bloquea la adopción, con condiciones** (solo `graphifyy` doble-y con oficialidad repo↔PyPI + pin + `uv`/`pipx` aislado; stdio siempre, prohibido http en red; 7/7 lectura por defecto con verificación de `triage_prs` + token GitHub read-only; no indexar carpetas con secrets + prompt defense).
   - **Decisiones del usuario (2026-09-19, registradas por `security-auditor`)**: (1) fuente cambiada a `Graphify-Labs/graphify` (la URL `tomasgraph/graphify` del manifest está muerta → 404; owner nuevo entra en allowlist + pin de versión a cargo de `devops`); (2) licencia dual Apache-2.0/MIT aceptada (ambas permisivas y compatibles, guardrail 8 satisfecho con nota de registro en manifest).
  - **Fase implementación**:
    - `devops`: ✅ **COMPLETADA (2026-09-18)** — `Configure-Graphify` real RF-19 en `scripts/plataformador-bootstrap.ps1` (solo ese script + manifest + este archivo tocados; bootstrap real NO ejecutado): árbol MCP-preferido — detecta `graphify` (Get-Command) + verifica `python -m graphify.serve --help` EXIT 0 (containment: solo registra ese comando exacto); registra Rama A en `opencode.json` (`type: local`, `command: [python, -m, graphify.serve, <root>/graphify-out/graph.json]`, `enabled: true`) + `.vscode/mcp.json` (`type: stdio`); sin CLI → WARN con instrucciones (cita manifest), no falla, no registra; sin grafo → registra igual + WARN con `graphify extract <path> --code-only` (servidor arranca sin grafo, `project_path` por herramienta); stdio siempre (cero `--transport http`), sin secrets; idempotente (conserva sin `-Force`); `Add-Member -Force` + IDictionary-aware (bug conocido); `-DryRun` informa, cero escrituras. Instalación real: PyPI `graphifyy` oficialidad repo↔PyPI verificada (project_urls → Graphify-Labs/graphify) + pin **0.9.48** (`uv tool install "graphifyy[mcp]"` aislado preferido por R-1 + `pip install "graphifyy[mcp]==0.9.48"` alineado; por qué uv: aislado, no contamina el Python global); `graphify --version` → 0.9.48; manifest corregido (URL real + tipo PyPI + licencia dual aceptada + nota 404 + historial). Handshake stdio real OK contra grafo de fixture (`extract --code-only`, 5 nodos/7 edges, sin secrets): `tools/list` → **10 herramientas** (no 7: upstream evolucionó — añade `get_community`, `god_nodes`, `graph_stats`; `triage_prs` existe) + `graph_stats` responde. `triage_prs` verificado solo-lectura en código (serve.py+prs.py: solo `gh pr list/view/diff` + `repo view`, prompt LLM "triage a PR review queue" es texto, cero POST/PATCH/PUT/DELETE; token = `gh auth` ambiente, nada en config) — apto documental, reporte para `qa-senior` sin bloqueo. Verificación: `Parser::ParseFile` 0 errores + 5 casos funcionales aislados ALL PASS (registro sin grafo, idempotencia, DryRun cero escrituras por hash, `-Force` con grafo, creación de mcp.json sin opencode.json; JSON resultantes válidos; fixtures en `$env:TEMP` limpiados).
    - `qa-senior`: Verificar handshake MCP o CLI + `get_stats`-equivalente si existe.
  - **Prioridad**: alta

- [ ] `[RELOCATE]` **Script nuevo `relocate-apps-to-src.ps1`: reubicación a `src\AppXXX` con confirmación obligatoria**
  - **Qué implementar**: Script NUEVO e independiente `scripts/relocate-apps-to-src.ps1` (decisión 2026-09-19: no tocar `plataformador-bootstrap.ps1` hasta tener todo OK). Traslada apps de la raíz a `src\<App>`, incluyendo `.venv`, con confirmación SIEMPRE obligatoria (sin bypass). Vale para cualquier proyecto (lista por `-AppDirs` o manifest). La integración al bootstrap viene DESPUÉS, como decisión separada tras validación completa.
  - **Confirmación obligatoria**: interactivo → pregunta por app (`[S]í mover / [N]o dejar / [T]odos los restantes / [C]ancelar todo`); no interactivo → NO mueve, informa; `-DryRun` → solo previsualiza. `plataformador-bootstrap.ps1` NO se modifica en esta tarea.
  - **Advertencia técnica (venv)**: mover un `.venv` rompe sus paths absolutos internos. Se mueve con la app pero se REPORTA como "a recrear" con comandos exactos. No se recrea solo.
  - **Controles**: pre-chequeos por app (origen existe, destino libre, tamaño, git limpio recomendado); log de cada movido; rollback manual documentado; post-movido advierte imports/paths/configs + tests; nunca toca `Documentacion/`.
  - **Basado en**: Decisiones del usuario (2026-09-19) — script separado hasta validación OK + confirmación siempre obligatoria.
  - **Fase documental**:
    - `documentador`: ✅ **COMPLETADA (2026-09-19)** — RF-13 (`Move-AppToSrc` + `Confirm-AppRelocation`; flags `-AppDirs`/`-DryRun`/`-ProjectRoot`, sin bypass; S/N/T/C, no-interactivo no mueve, venv a recrear, pre-chequeos + log + rollback + post-movido, nunca `Documentacion/`, bootstrap intacto) + criterio 9 en `specs/plataforma-bootstrap-instalador-unico/spec.md`; entrada T-D5 en `tasks.md`; sección Reubicación en `README.md` (versión 2026-09-19); adenda ADR-0003 (RNF-04 vs opt-in confirmado). Nuevo RF en la spec (script standalone + punto futuro de integración) + README (sección de reubicación) + ADR-0003 (RNF-04 vs opt-in confirmado).
    - `security-auditor`: ✅ **COMPLETADA (2026-09-19)** — Revisión de seguridad creada en `seguridad/relocate.md` (9 riesgos: mover código equivocado 🔴, corte a mitad del movido 🔴, venv en uso 🟠, fatiga de confirmación `[T]odos` 🟠, rollback incompleto 🟠, versionado parcial src 🟠, alcance fuera de lo sagrado 🟠, path traversal/escape 🟠, fuga por log 🟡). **Conclusión: no bloquea la adopción, con condiciones** (nombres simples + denylist + containment-check + destino libre, transacción por app + log + rollback concreto, sin bypass + no-interactivo no mueve, venv abortar si en uso + "a recrear", git limpio + commit atómico + tests).
    - `documentador`: ✅ **COMPLETADA (2026-09-19)** — Extensión auto-desactivar/reactivar venv + pausar git: RF-14 (`Suspend-AppLocks` + `Restore-AppLocks`; cada acción pregunta, sin bypass, `-Force` no aplica, No a todo = avisos, `-DryRun` informa sin mutar; venv en-sesión + `git.exe` puntuales con cwd verificado nunca `Code`; recrea + activa `src\<App>\.venv`, git se redescubre solo; mutar sesión del llamante documentado) + criterio 10 en `specs/plataforma-bootstrap-instalador-unico/spec.md`; entrada T-D6 en `tasks.md`; `README.md` actualizado (auto-desactivar/reactivar + pausar git, versión 2026-09-19).
    - `security-auditor`: ✅ **COMPLETADA (2026-09-19)** — Revisión RF-14 creada en `seguridad/relocate-locks.md` (complementa a `relocate.md` RF-13, formato heredado §§1-6 + referencias; 7 riesgos: kill `git.exe` equivocado por PID reutilizado 🔴, mutar sesión del llamante 🟠, `pip install` ejecuta código + red + tarda 🟠, reactivar venv ajeno por ruta mal construida 🟠, fatiga de confirmación 🟠, matar `Code` 🔴 evitado por diseño, fuga por log 🟡). **Conclusión: no bloquea, con condiciones** (revalidación atómica pre-kill Id+StartTime+cwd/exe + allowlist `git.exe` + denylist `Code` como innegociables; confirmación por acción sin bypass; dot-source solo con containment-check + `pyvenv.cfg` + verificación post-activación; `pip install` solo con confirmación separada + manifiesto fijado; `-DryRun`/no-interactivo sin mutación).
    - `documentador`: ✅ **COMPLETADA (2026-09-19)** — Limpieza de regenerables `[RELOCATE]` (plan aprobado 2026-09-19, decisión explícita: `.venv` se ELIMINA por comando, no se mueve; se recrea después en `src\<App>`): RF-15 (`Find-RegenerableDirs` + `Clear-RegenerableDirs`; allowlist fija Python + Node + general + `.venv/`; pregunta global de limpieza ANTES de S/N/T/C, No = mueve todo; `.venv` activo → `deactivate` previo obligatorio vía RF-14; log sin respaldo; recreación `python -m venv` + `pip install` en ruta nueva; nunca `Documentacion/`; `-DryRun` informa, no borra) + criterio 11 en `specs/plataforma-bootstrap-instalador-unico/spec.md`; entrada T-D7 en `tasks.md`; `README.md` actualizado (limpieza + `.venv` se elimina y recrea + `deactivate` previo, versión 2026-09-19).
     - `security-auditor`: ✅ **COMPLETADA (2026-09-19)** — Revisión RF-15 creada en `seguridad/relocate-cleanup.md` (complementa a `relocate.md` RF-13 y `relocate-locks.md` RF-14, formato heredado §§1-6 + referencias; 8 riesgos: no-regenerable por coincidencia de nombre 🔴, `.venv` ACTIVADO 🔴, `.venv` sin manifiesto irreproducible 🟠, `node_modules` con parches/bins offline 🟠, fatiga global+S/N/T/C+recreaciones 🟠, corte a mitad de limpieza 🟡, alcance fuera de lo sagrado 🟠, fuga por log/freeze 🟡). **Conclusión: no bloquea, con condiciones** (nombres exactos + nivel + gate git-trackeado que excluye con aviso + containment a apps candidatas; `deactivate` previo verificado fail-closed; manifiesto + `pip freeze` de rescate + confirmación separada para `.venv`; lockfile + `patches/` commiteado + aviso offline; global informada default No + `.venv` fuera del "sí a todo" + `[T]` por fase + no-interactivo no borra; log por app + recreación exacta en `src\<App>\` + tests; `qa-senior` valida con fixtures señuelo en `$env:TEMP`).
  - **Fase implementación**:
    - `devops`: Crear `scripts/relocate-apps-to-src.ps1` standalone (`Move-AppToSrc` + `Confirm-AppRelocation`; flags: `-AppDirs`, `-DryRun`, `-ProjectRoot`; SIN flag que saltee la confirmación). Cero cambios al bootstrap.
    - `devops`: ✅ **COMPLETADA (2026-09-18)** — Creado `scripts/relocate-apps-to-src.ps1` standalone (header + `param()` `-ProjectRoot`/`-AppDirs`/`-DryRun`, sin bypass; `#requires -Version 7.0`; estilo `Write-*` del bootstrap): `Confirm-AppRelocation` (S/N/T/C, no-interactivo → `N` + aviso, en DryRun no se invoca) + `Move-AppToSrc` (nombres simples + denylist 10 entradas + containment-check + symlinks, origen-existe/destino-libre, `.venv` → `NeedsVenvRecreate`, transacción por app + verificación post-movido + log OK/ABORTADO con rollback concreto, DryRun cero escrituras) + MAIN (`Invoke-AppRelocation`: lista numerada previa, `T` a restantes, `C` aborta todo, no-interactivo no mueve, informe final movidas/omitidas/venvs-a-recrear con `python -m venv .venv` + imports/tests + rollback inverso; nunca `Documentacion/`). Verificación: `Parser::ParseFile` 0 errores + suite de 42 checks PASS contra fixtures en `$env:TEMP` (DryRun no mueve, no-interactivo no mueve, movido directo OK con conteo, destino ocupado no sobrescribe, venv reportado, denylist/traversal/inexistente OMITIDA, `Confirm` no-interactiva `N`, bootstrap hash intacto `5E738EB9…`), fixtures limpiados. Corrección durante tests: llamada `Get-AppSizeInfo -LiteralPath` → `-LiteralDir` (L279).
    - `qa-senior`: ✅ **COMPLETADA (2026-09-19)** — Validación formal independiente de `scripts/relocate-apps-to-src.ps1`: `Parser::ParseFile` 0 errores + guard MAIN L501; DryRun en fixtures `$env:TEMP` informa todo (tamaño/venv) con cero escrituras (`src/` no creado, `Documentacion/` intacta); sin flag de bypass (solo `-ProjectRoot`/`-AppDirs`/`-DryRun`), no-interactivo → `N` fail-closed; denylist + nombres-simples 9/9 OMITIDA (+4 por código, containment + symlinks por código); movido real sobre fixtures preserva estructura (3=3) + rollback manual verificado + destino-ocupado no sobrescribe; bootstrap sin nada de relocate en su diff (0 matches); checklist `seguridad/relocate.md`: 17/18 PASS + 1 PARCIAL (H-1 no bloqueante: aviso de procesos Python sin abortar la app, a criterio de `devops`). Reporte: `Documentacion/Agents_IA_TECH/testing/validacion-relocate-2026-09-19.md`. **Conclusión: listo para uso real standalone** (pwsh ≥ 7, DryRun previo).
    - `devops`: ✅ **COMPLETADA (2026-09-19)** — Mejora aviso `.venv` ACTIVADO en `scripts/relocate-apps-to-src.ps1`: nueva `Test-ActiveVenv` (`-ProjectRoot`/`-AppDirs`; lee `$env:VIRTUAL_ENV` + `$env:CONDA_PREFIX` informativo; sin venv → `$null` en silencio; con venv → hashtable `@{ Active; Path; InApp; InProject; IsConda }` con prefijo canónico case-insensitive) + integración en el MAIN tras el STEP inicial y antes del plan (también en `-DryRun`, cero escrituras, no aborta): si está en una app a mover → `Write-Warn` con `deactivate` + reactivación en la ruta NUEVA `src\<App>\.venv\Scripts\Activate.ps1` (recrear primero, el movido queda roto); si está en el proyecto fuera de candidatas → aviso informativo; si está fuera → línea informativa. Verificación: `Parser::ParseFile` 0 errores + DryRun contra fixtures en `$env:TEMP` (con venv en fixture-app → aviso con `deactivate` + `src\` + `Activate.ps1`; sin venv → sin aviso; en-proyecto-no-app y fuera-del-proyecto → sus avisos; `src/` no creado, variable restaurada, fixtures limpiados).
    - `devops`: ✅ **COMPLETADA (2026-09-19)** — RF-14 `Suspend-AppLocks` + `Restore-AppLocks` en `scripts/relocate-apps-to-src.ps1` (solo ese script + este archivo tocados)
    - `devops`: ✅ **COMPLETADA (2026-09-19)** — RF-15 `Find-RegenerableDirs` + `Clear-RegenerableDirs` + `Invoke-RegenerableCleanup` en `scripts/relocate-apps-to-src.ps1` (solo ese script + este archivo tocados): `Find` (UN nivel, allowlist exacta Python+Node+general+`.venv`/`*.egg-info` por sufijo, nunca `Documentacion/`, gate git-trackeado que excluye con aviso "versionado, no se toca", flags sin-gate-git/activo/manifiesto/lockfile+`patches/`) + `Clear` (containment-check, log relativa+tamaño, idempotente, `.venv` activo → SALTA fail-closed, DryRun/no-interactivo no borra) + fase global en MAIN tras Suspend y ANTES del S/N/T/C (resumen consolidado, S/N default No, N = mueve todo, `.venv` con confirmación separada + aviso irreproducible + `pip freeze` de rescate previo si sin manifiesto) + informe con sección `.venv` ELIMINADOS (recrear en `src\<App>\` + `pip install` del manifiesto o del rescate). Verificación: `Parser::ParseFile` 0 errores + 20/20 checks lib en `$env:TEMP` (trackeado excluido, `dist-new`/`Documentacion/` no matchean, `.venv` activo fail-closed, DryRun cero escrituras, borrado real + reintento idempotente, manifiesto detectado) + E2E DryRun (lista consolidada antes del plan, cero escrituras, `src/` no creado). Fixtures + `$env:VIRTUAL_ENV` limpiados/restaurados.: `Get-AppGitLocks` (CIM `git.exe` + proxy CommandLine/exe en la app; allowlist `git.exe` + denylist `Code`; logs PID + relativa) + `Stop-VerifiedGitProcess` (revalidación atómica Id+StartTime+exe pre-kill, kill suave sin `-Force`) + `Suspend-AppLocks` (resumen previo; `¿Desactivo el venv X?`/`¿Pauso git.exe PID N?` por acción con S/N/[T] intra-acción/[C]; `deactivate` ausente = aviso sin fallar; DryRun/no-interactivo sin mutación; devuelve estado) + `Invoke-VerifiedVenvActivation` (containment + `Activate.ps1` + `pyvenv.cfg` + `VIRTUAL_ENV` confirmado) + `Restore-AppLocks` (según resultado real por app: movida→recrear con `¿Recreo?` + `pip install` SOLO con `¿Instalo?` + manifiesto fijado + `¿Activo?`; no movida→reactiva original; git se redescubre solo; No = avisos persistentes) + MAIN con `try/finally` (Suspend tras aviso venv y antes del plan; Restore siempre, sin bypass). Verificación: `Parser::ParseFile` 0 errores + suites en `$env:TEMP` (A DryRun y B no-interactivo: cero mutación; C 15/15 con venv falso: S desactiva/reactiva, N registra aviso, sin `deactivate` aborta sin fallar, movida+N no ejecuta python/pip, dot-source rechaza sin `pyvenv.cfg`/fuera-de-app; D 8/8: no-git vivo tras intento, guardas por código, daemon `git.exe` propio detectado + StartTime-distinto aborta + kill real revalidado muerto) + `pip install` jamás ejecutado en pruebas (N siempre + `python` bloqueado en sesión de test). Fixtures/procesos/variables limpiados.
    - `qa-senior`: ✅ **COMPLETADA (2026-09-19)** — Validación formal independiente RF-15 de `scripts/relocate-apps-to-src.ps1`: `Parser::ParseFile` 0 errores + carga segura (3 funciones, MAIN no corre al cargar); DryRun E2E con fixtures señuelo en `$env:TEMP` (allowlist exacta, `build/` trackeado excluido con aviso, `dist-new`/`Documentacion/` no matchean, `.venv` sin manifiesto + lockfile flaggeados, cero escrituras, `src/` no creado); borrado real de caches sobre fixtures + reintento idempotente ("ya limpio") con fuentes intactas; `.venv` activo simulado → fail-closed (skip con aviso, intacto, `$env:VIRTUAL_ENV` restaurado) + no-interactivo no borra; sin `-Force`/bypass; bootstrap sin diff de cleanup; checklist `seguridad/relocate-cleanup.md`: **15/15 PASS, 0 bugs**. Reporte: `Documentacion/Agents_IA_TECH/testing/validacion-relocate-cleanup-2026-09-19.md`. **Conclusión: listo para uso real** (pwsh ≥ 7, `-DryRun` primero, partir de git limpio).
  - **Fase integración (futura, decisión separada)**: solo tras validación OK se decide invocar el nuevo script desde el bootstrap.
  - **Limpieza de regenerables (decisión usuario 2026-09-19: APROBADA + `.venv` se ELIMINA, no se mueve)**: antes de mover, detectar y (con confirmación global única) eliminar directorios regenerables: Python (`__pycache__/`, `.pytest_cache/`, `*.egg-info/`, `.mypy_cache/`, `.ruff_cache/`, `build/`, `dist/`), Node (`node_modules/`, `.next/`, `dist/`, `build/`, `coverage/`), general (`.cache/`), **más `.venv/`** (decisión explícita: se elimina por comando y se vuelve a crear después en `src\<App>`, es lo más limpio). Requisito: si el `.venv` está ACTIVADO en la terminal, exigir `deactivate` primero (RF-14 `Suspend-AppLocks` ya lo cubre). Solo nombres exactos de lista fija (allowlist). Sin respaldo (regenerables) pero con log. Tras mover, el informe da los comandos de recreación (`python -m venv` + `pip install`) en la ruta nueva.
  - **Prioridad**: alta
  - **Hallazgos modo real Metatrader (2026-09-19, para `devops`/`qa-senior`)**: (1) `Move-Item` en `AppXXX` abortó seguro por lock transitorio (proceso `git` de VS Code sobre el `.git` anidado oculto, ya terminado; 0 errores de acceso, 0 archivos read-only; el `.git` oculto por sí solo no bloquea). Reintentar sin locks. (2) Discrepancia de conteo NO bloqueante: el plan informó 259 archivos en `AppXXX` pero hay 37 files + 19 dirs reales — revisar método de conteo de `Get-AppSizeInfo`. (3) El aviso de venv activo funcionó (`$env:VIRTUAL_ENV` detectado, comandos `deactivate`/reactivar mostrados).
  - **Auto-desactivar/reactivar venv + pausar git (pedido usuario 2026-09-19, documental ✅ 2026-09-19)**: que el script desactive solo los venv activos de apps a mover (con confirmación), pause los `git.exe` con cwd dentro de esas apps (con confirmación, nunca el proceso `Code`), y al terminar reactive (venv recreado en `src\` + git se redescubre solo). Sin bypass: cada acción destructiva pregunta. RF-14 + criterio 10 + T-D6 documentados; pendiente revisión de `security-auditor` (mutar sesión del llamante, matar procesos git) antes de implementar.

- [ ] `[BLINDAJE-GIT]` **Blindaje `.git` + alcance quirúrgico `.opencode` (kit-generico)**
  - **Qué implementar**: Guards transversales para mover+limpiar+sync: (RF-B1) `.git` intacto siempre (allowlist sin `.git` + denylist + gate git-trackeado + test obligatorio); (RF-B2) matriz de alcance `.opencode` (`agents/`+`commands/` versionar; `mcp.json`/`config.json`-plantilla generados por script; runtime `node_modules/`+`package*.json`+`bun.lock`+`lib/`+`bin/` ignorados; `config.json` con secrets ignorado, nunca loguear contenido); (RF-B3) `.gitignore` por nivel (raíz + anidado provisto por el kit vía sync); (RF-B4) destructivas solo sobre allowlist exacta + containment-check + fail-closed, prohibido `Remove-Item -Recurse` sobre raíz `.opencode`.
  - **Basado en**: Plan aprobado 2026-09-19 + `Documentacion/Agents_IA_TECH/specs/kit-generico/spec.md` (RF-B1..B4, 6 ACs).
  - **Hechos verificados**: `.git` nunca se borra (allowlist/denylist/gate); `AppXXX`/`AppYYY` nunca tuvieron `.git`; `.opencode/.gitignore` del kit creado 2026-09-19.
  - **Fase documental**:
    - `documentador`: ✅ **COMPLETADA (2026-09-19)** — Spec `specs/kit-generico/spec.md` (RF-B1..B4 + matriz + `.gitignore` por nivel + guards + 6 ACs + fuera de alcance + dependencias + referencias) + `tasks.md` (T-D/T-I/T-V) + esta entrada.
    - `security-auditor`: ✅ **COMPLETADA (2026-09-19)** — Revisión de seguridad creada en `seguridad/blindaje-git.md` (formato §§1-7; 5 riesgos: `config.json` commiteado 🔴, borrado fuera de allowlist 🟠, supply chain `lib/`/`bin/` 🟠, sync pisa `.gitignore` local 🟠, test sin `.git` anidados 🟡; hallazgos: anidado auto-ignorado L6, `robocopy /E` sin hash-guard en dirs, `lib/`/`bin/` vacíos sin pin). **Conclusión: no bloquea, con condiciones** (quitar auto-ignore + detección de trackeado con rotar, `ReparsePoint` por target + señuelos T-V3, pin+hash `lib/`/`bin/`, guard hash/`-Force` para `.gitignore` en sync, T-V1 triple raíz+anidado+lock).
  - **Fase implementación**:
    - `devops`: ✅ **COMPLETADAS T-I1..T-I3 (2026-09-19)** — T-I1: quitada línea 6 auto-ignore `.gitignore` de `.opencode/.gitignore` (archivo versionable, `git check-ignore` no lo matchea; runtime+`config.json`+`lib/`+`bin/` siguen ignorados). T-I2: hash-guard en `Sync-TransversalKit` para `.opencode/.gitignore` (si local difiere del maestro → WARN + conserva local, solo `-Force` sobrescribe; verifica línea `config.json` + restaura el anidado si falta + baseline `lib/`/`bin/` con WARN supply-chain; `-DryRun` informa sin mutar). T-I3: `Clear-RegenerableDirs` reforzado (re-validación allowlist exacta por target + veto `.git`/raíz `.opencode`/`Documentacion` + `ReparsePoint` por target real + containment-check; prohibido `Remove-Item -Recurse` sobre raíz `.opencode`) + detección `config.json` trackeado en sync (`git ls-files` → WARN con `git rm --cached` + rotar keys). Verificación: `Parser::ParseFile` 0 errores en ambos scripts + `git check-ignore` 5 patrones OK + `agents/` no ignorado + funcional aislada en `$env:TEMP` (limpiada) + `git status` solo los 4 paths.
    - `qa-senior`: T-V1 (test obligatorio `.git` intacto) + T-V2 (`ls-files`/`check-ignore`) + T-V3 (guards + fail-closed + sin secrets en logs).
  - **Prioridad**: alta

- [ ] `[HUERFANOS]` **Manejo de huérfanos en `Sync-TransversalKit` (preguntar: borrar o conservar con respaldo versionado)**
  - **Qué implementar**: Al sincronizar, detectar huérfanos (existen en `.github/` `.opencode/` `.doc_agents/` local pero ya no existen en el maestro) y PREGUNTAR al usuario por cada caso (o en lote): **¿borrar o conservar?** La implementación siempre queda limpia; la diferencia es si hay respaldo o no.
    - **Si borrar**: elimina los huérfanos y crea la implementación en limpio.
    - **Si conservar**: mueve cada huérfano a `revisar_manualmente\yyyymmdd\<ESTRUCTURA_ORIGINAL>` (fuera de los directorios de agentes, preservando la estructura de carpetas donde estaba), soportando varias versiones por fecha (si la carpeta del día existe, agrega sufijo de hora). Luego informa al usuario qué se movió y dónde quedó.
    - **Alcance**: solo archivos no propios o personalizados que causan conflicto (el usuario decide por archivo; opción "omitir" = dejarlo en su lugar por ser propio). Nunca tocar `Documentacion/<AppName>/`. Por defecto seguro (sin flag ni respuesta): **conservar** (nunca auto-borrar).
  - **Basado en**: Decisión del usuario (2026-09-18) — los huérfanos acumulados pueden hacer fallar el resto de la solución; la implementación debe quedar limpia siempre.
  - **Fase documental**:
    - `documentador`: ✅ **COMPLETADA (2026-09-18)** — RF-12 + criterio 8 + diagrama con paso de huérfanos en `specs/plataforma-bootstrap-instalador-unico/spec.md`; tasks T-D4/T-I7/T-I8/T-V6 en `tasks.md`; sección Actualización de `README.md` con pregunta borrar/conservar, respaldo `revisar_manualmente\yyyymmdd\`, default seguro y flag `-OrphanAction`.
    - `security-auditor`: ✅ **COMPLETADA (2026-09-18)** — Revisión de seguridad creada en `seguridad/huerfanos.md` (8 riesgos: borrar archivo equivocado 🔴, secrets en respaldo + `revisar_manualmente/` versionada 🔴, fuga por log 🟠, borrado no interactivo 🟠, path traversal en respaldo 🟠, alcance fuera de allowlist 🟠, huérfano malicioso 🟡, colisión del respaldo 🟡). **Conclusión: no bloquea la adopción, con condiciones** (default Conservar + confirmación para Borrar, `revisar_manualmente/` en `.gitignore`, containment-check, log sin contenido, detector acotado a allowlist).
  - **Fase implementación**:
    - `devops`: T-I7 ✅ **COMPLETADA (2026-09-18)** — `Find-OrphanKitFiles` (detección vs clon maestro, allowlist 3 dirs, excluye `.opencode/config.json`, throw si maestro incompleto, solo lectura) + `Invoke-OrphanDecision` (pregunta lote/uno-por-uno con doble confirmación para Borrar, flag `-OrphanAction Borrar|Conservar|Preguntar`, Borrar no interactivo exige `-Force`, movido versionado a `revisar_manualmente\yyyymmdd\` con containment-check + nunca sobrescribir, `-DryRun` solo informa) + helpers `Show-OrphanList`/`Remove-OrphanFiles`/`Move-OrphanFilesToBackup`, integrados en `Sync-TransversalKit` (llamada dentro del `try`, antes del `finally`) + flag CLI `-OrphanAction` (default `Preguntar`). Cumple las 6 condiciones de `seguridad/huerfanos.md`. Verificación: `Parser::ParseFile` 0 errores + DryRun sin efectos (`git status` idéntico).
    - `devops`: T-I8 ✅ **COMPLETADA (2026-09-18)** — `revisar_manualmente/` agregada a `.gitignore` (sección propia, no versionar — pueden contener secrets) + el script advierte si no está ignorada en cada movido.
    - `qa-senior`: T-V6 ✅ **COMPLETADA (2026-09-18)** — DryRun bootstrap + wrapper EXIT 0 con `git status` idéntico y sin `revisar_manualmente/`; `Find-OrphanKitFiles` detecta exactamente 3/3 huérfanos en fixtures TEMP (solo-local, subcarpeta, `config.json` excluido, allowlist respetada); maestro incompleto/inexistente → throw; guardas `-DryRun` verificadas por código + ejecución; `Move-OrphanFilesToBackup` real sobre fixtures preserva `revisar_manualmente\yyyymmdd(-HHmmss)\<estructura>` sin sobrescribir (OLD intacto) + `Remove` loguea hash y rechaza `..`; `git check-ignore` OK (L22); checklist §5: 17/18 PASS. Hallazgos no bloqueantes para `devops`: H-1 informe de conservados sin hash SHA256 por archivo (ítem 14 parcial), H-2 agentshield Grade C por 262 criticals preexistentes en `proyect_ext/tokenslayer/package-lock.json` (tercero gitignored, fuera de alcance). Reporte: `Documentacion/Agents_IA_TECH/testing/validacion-huerfanos-2026-09-18.md`. **Conclusión: listo para rollout** (pwsh ≥ 7, DryRun previo).
  - **Prioridad**: alta

- [ ] `[README-DESPLIEGUE]` **Reescribir `README.md` como guía única de despliegue (primera vez + actualizaciones)**
  - **Qué implementar**: Reescribir `README.md` con esta estructura obligatoria: (1) explicación de lo que hace la instalación, (2) instalación (primera vez), (3) actualización, (4) resto de la información. Actualizar contenido al diseño real: flujo bootstrap (`plataformador-bootstrap.ps1` como instalador/actualizador único), repo maestro `https://github.com/BUSCADO-LA-VIDA/Agents_IA_TECH` (sin placeholders `TU_USER`), 11 agentes, OpenCode + VS Code, MCPs, política merge-no-mirror (`-DryRun`/`-Force`, huérfanos), `.gitignore` (qué no se versiona).
  - **Cláusula de mantenimiento (obligatoria)**: el documento debe incluir una sección que indique que cuando se actualicen procesos que puedan afectar a este documento, el documento debe actualizarse también (evita que quede desactualizado como ahora).
  - **Versión del documento**: incluir texto de versión con fecha (ej. `Versión: 2026-09-18`).
  - **Basado en**: Decisión del usuario (2026-09-18) — estructura + cláusula + versión.
  - **Fase documental**:
    - `documentador`: Reescribir `README.md` según estructura y requisitos.
    - `security-auditor`: Solo si incluye comandos de descarga/ejecución (revisar URLs e invocaciones).
    - `security-auditor`: ✅ **COMPLETADA (2026-09-18)** — Auditoría PASS 4/4 (URLs confiables + repo maestro correcto sin placeholders; sin petición de secrets; `-DryRun` antes de modo real y `-Force` advertido; `winget` solo como texto de recomendación manual). Sin hallazgos que corrijan el `documentador`.
  - **Prioridad**: alta

- [ ] `[TOKENSLAYER]` **Registrar `tokenslayer-mcp-server` como MCP en ambos harnesses y documentarlo en el kit**
  - **Qué implementar**: TokenSlayer (`ajvikram.tokenslayer` v1.5.0, MIT) está instalado en VS Code pero opencode NO puede usar extensiones `.vsix` (runtimes distintos). El camino es su MCP server standalone (`https://github.com/ajvikram/TokenSlayer`, carpeta `mcp-server/`): clonarlo, compilarlo (`node` v24.14.0 disponible) y registrarlo en `opencode.json` + `.vscode/mcp.json`. Expone `analyze_files`, `analyze_workspace`, `analyze_dependency_chain`, `expand_node`, `apply_patch`, `get_stats`, `clear_stats` (esqueletos AST, 40-95% menos tokens). Complementa a `context-mode` (docs) y `codebase-memory-mcp` (grafo), no los reemplaza.
  - **Basado en**: Plan aprobado por el usuario (2026-09-18) + investigación (marketplace + GitHub ajvikram/TokenSlayer).
  - **Decisiones del usuario (2026-09-18)**: plan aprobado sin cambios.
  - **Fase documental**:
    - `documentador`: ✅ **COMPLETADA (2026-09-18)** — Guía `MCPs/tokenslayer.md` creada (qué es, instalación clonar + compilar `mcp-server/`, configuración esperada en `opencode.json` + `.vscode/mcp.json` con `"type": "stdio"`, uso de las 7 herramientas, pipeline de compactación previa, mantenimiento, seguridad pendiente de `security-auditor`, validación). Registrado en `dependencias-manifest.yml` (`tokenslayer-mcp-server` v1.5.0, MIT ✅) + entrada en historial. Actualizados `00-indice.md` (estructura `MCPs/` + tabla de MCPs) y `referencias.md` (repo ajvikram/TokenSlayer, MIT).
    - `security-auditor`: ✅ **COMPLETADA (2026-09-18)** — Revisión de seguridad creada en `seguridad/tokenslayer.md` (5 riesgos: `apply_patch` 🟠, supply chain clon+npm 🟠, secrets en esqueletos 🟠, API key propia en extracción semántica 🟡, stats en `~/.tokenslayer/stats.jsonl` 🟡; licencia MIT ✅). **Conclusión: no bloquea la adopción, con condiciones** (`dryRun` primero + diff revisado, versión fijada v1.5.0 + hash verificado, no indexar carpetas con secrets).
  - **Fase implementación**:
    - `devops`: ✅ **COMPLETADA (2026-09-18)** — Clonado `https://github.com/ajvikram/TokenSlayer` con versión fijada v1.5.0 en `proyect_ext/tokenslayer` (commit `9a380c041209e5599ad63ffd897dc99c7936327e`); `npm install` + `npm audit` en `mcp-server/` (0 críticas; 6 no bloqueantes: 1 low/2 moderate/3 high, transitivas de `@modelcontextprotocol/sdk`, no se corrigen); `npm run build` OK (`build/index.js` generado); handshake MCP verificado por stdio (`initialize` → `tokenslayer-mcp-server` 1.3.0 + `tools/list` → 8 herramientas, sin API keys). Registrado en `opencode.json` (clave `tokenslayer`, `type: local`, node absoluto + script compilado, `enabled: true`) y `.vscode/mcp.json` (servidor `tokenslayer`, `command: node` + `args`, `type: stdio`); ambos JSON validados. Sin secrets en configs/docs/logs; no se indexaron carpetas con secrets; `apply_patch` no ejecutado.
    - `qa-senior`: ✅ **COMPLETADA (2026-09-18)** — Probados `analyze_files` + `expand_node` + `get_stats` por stdio contra `build/index.js` (sin `apply_patch`, sin `clear_stats`, sin secrets, sin keys). `parser.ts` 92% (11400→869), `stats.ts` 81% (4542→864), `dashboard.ts` 99% con aviso low-yield honesto del propio server; `expand_node` devuelve fuente exacto; `get_stats` 20594 ahorrados / 92% en 3 análisis (aritmética verificada). Ahorro dentro del claim 40-95%. Hallazgos devops confirmados: `serverInfo` 1.3.0 (es la versión del MCP server; v1.5.0 es la extensión/tag) y 8 herramientas (extra `session_health`). Deuda no bloqueante para `documentador`: actualizar `MCPs/tokenslayer.md` (8 herramientas, params reales `filePaths`+`expandable`+`targetModel`, aclarar versiones). Reporte: `Documentacion/Agents_IA_TECH/testing/validacion-tokenslayer-2026-09-18.md`. **Conclusión: listo para uso en modo solo-lectura.**
  - **Bugs QA corregidos (2026-09-18)**: BUG-1 (`session_health` agregada a la guía, 7→8 herramientas) + BUG-2 (params reales `filePaths` requerido + `expandable` + `targetModel` en §5, `expand_node` aclarado como herramienta independiente) ✅ por `pensador`; BUG-3 (skew tag v1.5.0 vs server interno 1.3.0 aclarado en manifest + historial) ✅ por `devops`.
  - **Política de versionado (decisión usuario 2026-09-18)**: `proyect_ext/tokenslayer/` NO se versiona (código duplicado de terceros, no propio) y `.vscode/mcp.json` tampoco (se genera con los scripts). Ambos en `.gitignore` (tokenslayer explícito; mcp.json cubierto por `.vscode/`). Se controlan por fuera y **se regeneran desde el script** (`plataformador-bootstrap.ps1`: clona + compila + registra). No aportan valor al repo. `spec-kit` y futuros clones de `proyect_ext/` se evalúan caso por caso.
  - **Repo privado (2026-09-19)**: README actualizado con instrucciones autenticadas (prerrequisito `gh auth login`, Vía A con `gh api`, nota del 404 en descarga anónima, auth necesaria para `git clone` en sync, Vías B/C alternativas). Versión del documento: 2026-09-19.
  - **Solo-git (decisión usuario 2026-09-19)**: sin `gh` ni herramientas extra. Flujo con `git` únicamente: Git Credential Manager (incluido en Git para Windows, login por navegador una vez y cachea) + `sparse-checkout` para traer solo los scripts sin clonar todo. README actualizado a Vía A sparse-checkout; Vía B copia local; PAT solo como último recurso documentado (nunca en docs/scripts versionados).
  - **Clon temporal fuera de la raíz (decisión usuario 2026-09-19)**: la Vía A clonaba en la raíz (`agents-temp`) y ensuciaba el proyecto. Ahora clona en `proyect_ext\agents-temp` (ideal) o `$env:TEMP` (alternativa válida), se copian los 2 scripts y se elimina el temporal. La raíz queda limpia y el script hace su trabajo. README actualizado; versión del documento 2026-09-19.
  - **Fix `--no-cone` (error real 2026-09-19, plan aprobado por el usuario)**: `sparse-checkout set` en modo cono solo acepta directorios; con patrones de archivo falla (`is not a directory... rerun with --skip-checks`) y `scripts/*.ps1` no se trae (solo archivos de raíz como `sync-agents.ps1`). Solución documentada: `sparse-checkout set --no-cone <archivos>`. README Vía A corregida.
  - **BUG mensaje final (DryRun real en Metatrader 2026-09-19, para `devops`)**: ✅ **CORREGIDO (2026-09-19, `devops`)** — backticks eliminados en `scripts/plataformador-bootstrap.ps1` (líneas 1873-1874 del MAIN). El bloque "Siguientes pasos recomendados" salía con quoting roto (`1. Revisar Documentacion/00-indice.md" -ForegroundColor White` + `Write-Host 2. Validar...` literal). Causa: backtick/escape mal cerrado en los `Write-Host` finales del MAIN. Cosmético pero debe corregirse. Secundarios: `True` suelto tras cada "Comando disponible" (retorno de `Ensure-Command` no suprimido) y mojibake en salida de `context-mode doctor` (codepage de consola, no del script).
  - **Prioridad**: media

- [ ] `[PLATAFORMA]` **Implementar Spec-kit + MCPs + Graphify en todos los proyectos, con `plataformador-bootstrap.ps1` como instalador/actualizador único**
  - **Qué implementar**: Convertir `plataformador-bootstrap.ps1` en el instalador/actualizador único que: (1) implementa Spec-kit en todos los proyectos (incluido este como principal), (2) implementa los MCPs para todos los harnesses, (3) activa y usa los MCPs junto a Graphify y las aplicaciones de apoyo, (4) descarga las apps desde sus repos git y las coloca en `src\AppXXX\`, (5) crea `.specify` + `Documentacion\<AppName>\` por app, (6) resuelve rutas de spec-kit por app activa, (7) valida que el flujo se respeta desde la carga de VS Code sin re-ejecutar scripts.
  - **Basado en**: Plan aprobado por el usuario (2026-09-17) — 6 prioridades + decisiones de diseño.
  - **Decisiones del usuario (2026-09-17)**:
    - **`sync-agents.ps1` → Opción A**: absorber su lógica dentro de `plataformador-bootstrap.ps1` como función `Sync-TransversalKit`; `sync-agents.ps1` queda como wrapper. Documentar la fusión.
    - **Repo maestro**: `https://github.com/BUSCADO-LA-VIDA/Agents_IA_TECH`
    - **Apps como aplicaciones** (con su propio `.specify` y doc): `MiApp`, `AppFoo`, `AppBar`, `AppBaz`, `AppQux`. **`proyect_ext/spec-kit` queda en la raíz** (es de apoyo, no se mueve a `src\`).
    - **Resolución de app activa**: por **directorio de trabajo actual** + **flag `-App`**.
  - **Fase documental**:
    - `arquitecto`: ✅ **COMPLETADA (2026-09-17)** — ADR-0003 creado en `arquitectura/adr/adr-0003-plataforma-bootstrap-instalador-unico.md` (modelo apps independientes + orquestador, estructura objetivo `src\AppXXX\`, resolución de app activa por `-App` + `cwd`, fusión de `sync-agents.ps1` como `Sync-TransversalKit` — Opción A, guardrails, spec linking, diagrama Mermaid, plan por fases). Actualizados `00-indice.md` (ADRs activos) y `pendientes-implementacion.md`.
    - `documentador`: ✅ **COMPLETADA (2026-09-17)** — Documentado el rol del bootstrap como instalador/actualizador único + fusión de `sync-agents.ps1` (Opción A). Creada la feature `specs/plataforma-bootstrap-instalador-unico/` con `spec.md` (objetivo, RF-01..RF-11, RNF-01..RNF-08, criterios de aceptación, diagrama Mermaid reutilizado del ADR-0003), `plan.md` (fases documental → implementación → validación con tareas de `devops`/`qa-senior`) y `tasks.md` (T-D1..T-V5 desglosadas). Actualizados `00-indice.md` (ADR-0003 en ADRs activos + feature en specs), `referencias.md` (repo maestro `https://github.com/BUSCADO-LA-VIDA/Agents_IA_TECH` como fuente) y `pendientes-implementacion.md`.
    - `security-auditor`: ✅ **COMPLETADA (2026-09-17)** — Revisión de seguridad creada en `seguridad/plataforma-bootstrap.md` (6 riesgos: supply chain 🔴, secrets en configs 🔴, ejecución de scripts descargados 🔴, URLs maliciosas 🟠, licencias sin verificar 🟠, sobrescritura de `Documentacion/<AppName>/` 🟠). **Conclusión**: no bloquea la adopción, condicionado a: (1) manejar solo configs sin secrets y excluir `.opencode/config.json`, (2) no ejecutar scripts descargados sin revisión, (3) fail-closed en URLs (solo github.com, owner en allowlist) y licencias, (4) `qa-senior` valide con `-DryRun` en proyecto real antes del rollout. ✅ **CERRADA (2026-09-18)** — Licencia de `graphify` verificada: **MIT** (fuente https://graphify.net/es/ sección "Security, Licensing & Trust"; deps NetworkX BSD + Tree-sitter MIT, sin telemetría). Guardrail 8 satisfecho para graphify. Manifest actualizado.
     - `documentador`: ✅ **COMPLETADA (2026-09-19)** — Extensión 3 gaps de auditoría real de Metatrader (plan aprobado 2026-09-19): RF-16 (`Ensure-OpenCodeMcp` registra tokenslayer como 4º MCP, `type: local` + `command: [node, <repo>/proyect_ext/tokenslayer/mcp-server/build/index.js]` + `enabled: true`, fuente entrada `tokenslayer-mcp-server` en `dependencias-manifest.yml`; sin binario → WARN + instrucciones de compilar, no falla) + RF-17 (`Ensure-OpenCodeConfig` crea `.opencode/config.json` SOLO si no existe desde plantilla con PLACEHOLDERS, si existe no lo toca nunca, sigue gitignored, keys a mano documentadas) + RF-18 (`Repair-DocStructure` mueve docs sueltas de raíz a `Documentacion/<App>/` con confirmación por archivo S/N/T/C sin bypass, >50KB/tracking vivo solo se listan, nunca borra, `-DryRun` informa) + criterios 12/13/14 en `specs/plataforma-bootstrap-instalador-unico/spec.md`; entrada T-D8 en `tasks.md`; `README.md` actualizado (4º servidor + troubleshooting `config.json`, versión 2026-09-19).
    - `security-auditor`: ✅ **COMPLETADA (2026-09-19)** — Revisión RF-16/17/18 creada en `seguridad/kit-gaps.md` (complementa a `plataforma-bootstrap.md` y `tokenslayer.md`, formato heredado §§1-6; 6 riesgos: plantilla→secret commiteado 🔴, binario inexistente/comprometido 🔴, tracking vivo por click-through 🟠, archivo abierto por otro proceso 🟠, alcance fuera de lo sagrado 🟠, fuga por log 🟡). **Conclusión: no bloquea, con condiciones** (placeholders inconfundibles + recuerdo gitignore + detección de trackeado con aviso de rotar; WARN sin auto-compilación + containment-check + pin en manifest; tracking vivo fuera del `[T]` + S/N individual; movido transaccional por archivo + locks por archivo; `qa-senior` valida con fixtures en `$env:TEMP`).
  - **Fase implementación**:
    - `plataformador`: ✅ **COMPLETADA (2026-09-18)** — Refactorizado `plataformador-bootstrap.ps1`: `Sync-TransversalKit`, resolución de app activa (`-App` + `cwd`), integración de Spec-kit y Graphify, `.specify` + doc por app, preparación de apps en `src\AppXXX\`, respetando guardrails del ADR-0003.
    - `devops`: ✅ **COMPLETADA (2026-09-18)** — `sync-agents.ps1` convertido en wrapper que delega en `Sync-TransversalKit` (DryRun verificado, EXIT 0).
    - `qa-senior`: ✅ **COMPLETADA (2026-09-18)** — Tests DryRun (cero escrituras, frontera `Documentacion/<AppName>/` respetada).
    - `devops`: ✅ **COMPLETADA (2026-09-18)** — Fix `Ensure-OpenCodeMcp` en `scripts/plataformador-bootstrap.ps1` (líneas 295 y 306): asignación directa `$existing.mcp` / `$existing.plugin` → `Add-Member -NotePropertyName ... -NotePropertyValue ... -Force` (bug real en Metatrader: crear propiedades nuevas por asignación falla en pwsh 7.6.6 con `SetValueInvocationException`; `Add-Member -Force` validado end-to-end). Auditoría de todo el script con `Select-String '\$\w+(\.\w+)+\s*='`: solo esas 2 ocurrencias del patrón, ambas corregidas; lógica (hash/`Force`/`DryRun`/plugin-check) intacta. Verificación: `Parser::ParseFile` 0 errores + prueba funcional aislada (función cargada sin ejecutar el MAIN) contra copia temporal del `opencode.json` de Metatrader (sin `mcp` ni `plugin`): agrega ambas secciones (3 MCPs + `plugin: context-mode`), JSON resultante válido, segunda ejecución idempotente, temporal en `$env:TEMP` limpiado.
     - `devops`: ✅ **COMPLETADA (2026-09-19)** — Refinamiento `Reload-ProjectWindow` en `scripts/plataformador-bootstrap.ps1` (problema real detectado por el usuario: si el script corre desde el terminal integrado del VS Code que va a recargar, el reload mata el terminal y rompe la ejecución). Detección del entorno ANTES de invocar `code --reuse-window`: si `$env:TERM_PROGRAM -eq 'vscode'` → NO ejecuta `code`, muestra `Write-Warn` (explica que recargar mataría este mismo terminal) + instrucción manual (`Ctrl+Shift+P` → `Developer: Reload Window` en ESTA ventana al terminar) y retorna sin error; si es terminal externa (Windows Terminal, pwsh.exe, etc.) → `code --reuse-window "<RootPath>"` como antes (seguro: la terminal externa sobrevive). Rama `$NoRestart`/`$DryRun` intacta. Mensajes claros en las 3 ramas (omitido / auto-recarga externa / recarga manual requerida), siempre "recargar", nunca "reiniciar". Verificación: `Parser::ParseFile` 0 errores; DryRun `-DryRun -NoRestart -SkipInstall -SkipIndexing` → EXIT 0.
      - `devops`: ✅ **COMPLETADA (2026-09-19)** — Fix BUG mensaje final `[PLATAFORMA]` en `scripts/plataformador-bootstrap.ps1` (líneas 1873-1874 del MAIN): eliminados los backticks que escapaban la comilla de cierre (`` `Documentacion/...md` `` → `Documentacion/...md`), causa del quoting roto (`...md" -ForegroundColor White` + `Write-Host 2. ...` literal). Contenido de los 6 pasos intacto, solo quoting. Verificación: `Parser::ParseFile` 0 errores + bloque final ejecutado de forma AISLADA desde temporal en `$env:TEMP` (6 líneas verificadas, temporal limpiado).
       - `devops`: ✅ **CORREGIDO (2026-09-19)** — Fix BUG `plugin: [null, "context-mode"]` `[PLATAFORMA]` en `Ensure-OpenCodeMcp` (`scripts/plataformador-bootstrap.ps1`): bug real en producción (Metatrader, `opencode` rechaza la config con `Expected string | array, got null plugin.0`). Causa: `@(@($existing.plugin) + "context-mode")` — cuando `plugin` no existe, `@($null)` produce un elemento null. Reemplazado por construcción que filtra nulos/vacíos y evita duplicados (`$currentPlugins = @(@($existing.plugin) | Where-Object { $_ -ne $null -and $_ -ne "" })` + `-notin` + `Add-Member -Force`); resto de la lógica (chequeo hasPlugin, DryRun, mensajes) intacta. Verificación: `Parser::ParseFile` 0 errores + 3 pruebas funcionales aisladas (función cargada sin ejecutar el MAIN, temporales en `$env:TEMP` limpiados): SIN `plugin` → `["context-mode"]` sin nulls; YA con `["context-mode"]` → idempotente sin duplicar; con `["otro"]` → `["otro","context-mode"]` (preserva + agrega); los 3 JSON resultantes válidos.
      - `devops`: ✅ **COMPLETADA (2026-09-19)** — RF-16/17/18 `[PLATAFORMA]` en `scripts/plataformador-bootstrap.ps1` (solo ese script + este archivo tocados; bootstrap real NO ejecutado; sin `config.json` reales creados): RF-16 extiende `Ensure-OpenCodeMcp` con 4ª entrada `tokenslayer` (`type: local`, `command: [<node-absoluto vía Get-Command node>, <root>/proyect_ext/tokenslayer/mcp-server/build/index.js]`, `enabled: true`; si falta node → WARN + omite sin fallar; sin binario → WARN con instrucciones clonar+compilar citando `dependencias-manifest.yml` y NO registra; containment-check bajo `proyect_ext/tokenslayer/`; conserva si ya existe salvo `-Force`; entrada dict-aware `IDictionary` vs `Add-Member -Force`; estilos/`$DryRun`/`$Force` intactos) + RF-17 nueva `Ensure-OpenCodeConfig` (crea `.opencode/config.json` SOLO si no existe desde plantilla con PLACEHOLDERS `__PEGAR_AQUI_TU_...__` + aviso inline, estructura derivada de las keys `{env:...}` de `opencode.json` sin valores reales; si existe no lo toca nunca; verifica gitignored + detección defensiva de trackeado con `git rm --cached` + rotar; `-DryRun` informa) + RF-18 nuevas `Repair-DocStructure` + helper `Move-SingleDocFile` (allowlist exacta raíz-profundidad-0 incl. `pendientes-*.md`/`preferencias*.md`, nunca `README.md`/`AGENTS.md`, nunca `Documentacion/` como fuente; destino `Documentacion/<App>/` por `-App`>`cwd`>pregunta/lista; S/N/T/C sin bypass, `-Force` no aplica; tracking vivo/>50KB fuera del `[T]` con S/N individual + advertencia + default No; transaccional por archivo con rollback concreto; lock→omite+informa+continúa; nunca borra; `-DryRun`/no-interactivo solo listan; logs rutas relativas) + MAIN: `Ensure-OpenCodeConfig` en paso 4 y `Repair-DocStructure` en paso 7b (tras `Prepare-Apps`, antes de indexar). Verificación: `Parser::ParseFile` 0 errores + suite aislada de 44 checks ALL PASS (funciones cargadas sin MAIN, fixtures en `$env:TEMP` limpiados): MCP sin `mcp`→4 entradas incl. tokenslayer + idempotente, sin binario→3+WARN sin registrar, mcp previo conservado + tokenslayer agregado, DryRun sin escrituras; plantilla creada con placeholders sin valores reales + existente intacta; DryRun/no-interactivo listan sin mover, `README`/`AGENTS` no cazados, movido real OK + destino-existente no sobrescribe + lock omitido con original intacto. `git status` limitado a los 2 paths. ⏳ **Pendiente validación `qa-senior` (incl. interactivo S/N/T/C real y `pendientes-87KB` fuera del `[T]`).**
  - **Fase validación del flujo**:
    - `qa-senior`: ✅ **COMPLETADA (2026-09-18)** — DryRun en Agents_IA_TECH y Metatrader (EXIT 0, sin tocar `Documentacion/`); Spec-kit/MCPs verificados estáticamente (`opencode.json` plugin+3 MCPs, hooks Pre/Post/SessionStart, `.vscode/mcp.json` 3 servidores); `ecc-agentshield scan` Grade A (98/100, 0 critical/high). Reporte: `Documentacion/Agents_IA_TECH/testing/validacion-bootstrap-2026-09-18.md`. Hallazgo no bloqueante: el script requiere **pwsh ≥ 7** (no parsea en PowerShell 5.1); licencia `graphify` sigue pendiente (fail-closed por diseño). **Conclusión: rollout SÍ con condiciones** (pwsh 7, DryRun previo, cerrar licencia graphify).
    - `qa-senior`: Verificar que cualquier proceso pedido respeta los flujos (Plan → Document → Implement).
  - **Prioridad**: alta

- [ ] `[MCP-ACTIVAR]` **Activar y usar los MCPs como herramienta primaria de consulta (ahorrar tokens)**
  - **Qué implementar**: Los MCPs (`context-mode`, `codebase-memory-mcp`, `markitdown`) están **instalados y configurados** en `.vscode/mcp.json`, pero **NO están disponibles como herramientas para los agentes** en la sesión actual. Por eso los agentes leen archivos directos (gastando más tokens). Este plan define lo que falta para **instalar, configurar y USAR** los MCPs de verdad.
  - **Basado en**: Plan aprobado por el usuario (2026-09-12) — activar MCPs + usar flujo de contexto. Decisión del usuario: "Si lees directo gastas más tokens. Define en el PLAN lo que falta primero para que instales, configures y uses los MCP."
  - **Fase A — Activar y verificar los MCPs (PRIORIDAD 1)**:
    - `plataformador`: Verificar instalación real de `context-mode`, `codebase-memory-mcp`, `markitdown-mcp` (responden `--version` y están en el `PATH`).
    - `plataformador`: **Reiniciar sesión de Copilot** (recargar ventana VS Code) para que `.vscode/mcp.json` se cargue y los MCPs se expongan como herramientas.
    - `plataformador`: Verificar que los MCPs aparecen como herramientas disponibles (`ctx_*`, `index_repository`, `query`, `semantic_search`, `convert_to_markdown`).
    - `plataformador`: Probar cada MCP con una consulta de prueba.
  - **Fase B — Configurar el uso de MCPs en los agentes (PRIORIDAD 2)**:
    - `documentador`: Actualizar `.github/copilot-instructions.md` con instrucción explícita: usar los MCPs (`ctx_search`, `index_repository`, `query`, `semantic_search`, `convert_to_markdown`) para consultar documentación/código en lugar de leer archivos directos cuando sea posible (ahorra tokens).
    - `documentador`: Actualizar specs de agentes con la instrucción de usar MCPs como herramienta primaria de consulta (no solo "optimización opcional").
    - `documentador`: Definir el flujo de re-indexación forzada (primera consulta → forzar re-indexación sin IA → luego consultar por MCP).
  - **Fase C — Completar los demás procesos (PRIORIDAD 3)**:
    - `upgrade_framework`: Flujo de actualización automática de herramientas externas.
    - `plataformador`: Implementar flujo de contexto con re-indexación forzada.
    - `gitflow`: Commits.
  - **Prioridad**: alta

- [ ] `[FLUJOS]` **Alinear flujos del kit: contexto (consultar Documentacion/) + actualización automática de herramientas externas**
  - **Qué implementar**: Definir y documentar dos flujos que faltan para que el kit funcione de forma coherente:
    1. **Flujo de contexto**: Todos los agentes consultan `Documentacion/Agents_IA_TECH/` (fuente de verdad) para saber en qué punto de la solución estamos. Los MCPs (context-mode, codebase-memory-mcp, markitdown) **optimizan** esa consulta, NO la reemplazan. Incluye **actualización de memoria/índice** cuando cambia la documentación (los MCPs no siempre están actualizados).
    2. **Flujo de actualización automática de herramientas externas**: Al agregar/quitar herramientas externas, `upgrade_framework` sabe exactamente qué hacer (no como hoy con graphify/MCP). El flujo concreto se define al momento de la implementación.
  - **Basado en**: Plan aprobado por el usuario (2026-09-12) — alinear flujos del kit
  - **Fase documental**:
    - `arquitecto`: ✅ **COMPLETADA (2026-09-12)** — ADR-0002 creado en `arquitectura/adr/adr-0002-flujos-kit.md` (contexto, decisión de ambos flujos, consecuencias, guardrails, spec linking, diagrama Mermaid **Flujo de contexto**, plan por fases). Actualizados `00-indice.md` (ADRs activos) y `pendientes-implementacion.md`.
    - `documentador`: ✅ **COMPLETADA (2026-09-12)** — Documentado el **flujo de contexto** (ADR-0002) en las specs de **todos los agentes** del kit: `pensador`, `plataformador`, `analista_tecnico`, `arquitecto`, `documentador`, `security-auditor`, `api-developer`, `frontend-developer`, `devops`, `qa-senior`, `gitflow` y `solucionador`. En cada spec se agregó una sección **"Flujo de contexto (ADR-0002)"** con: archivos de entrada obligatorios al iniciar una tarea (`00-indice.md`, `pendientes-implementacion.md`, `memoria-proyecto.md`, `preferencias.md`, `idioma.md`), la regla de que los MCPs (`context-mode`, `codebase-memory-mcp`, `markitdown`) **optimizan y NO reemplazan** la fuente de verdad, el orden de consulta (primero doc directa, luego MCPs), la **actualización de memoria/índice** (re-indexar + actualizar `analisis-memoria.md`) y el diagrama **Flujo de contexto** reutilizado del ADR-0002. En `pensador` se agregó además la responsabilidad 15 (flujo de contexto). En los implementadores se adaptó la lista de archivos a su rol (specs de feature, soluciones-conocidas en `solucionador`, sección "Completadas" en `gitflow`).
    - `security-auditor`: ✅ **COMPLETADA (2026-09-12)** — Revisión de seguridad de uso del **flujo de contexto** (ADR-0002, Flujo 1) creada en `Documentacion/Agents_IA_TECH/seguridad/flujo-contexto.md`. Analiza los 4 momentos de riesgo del flujo: **M-1** consultar `Documentacion/` (riesgo 🔴 Crítico de secrets/credenciales en la doc expuestos a todos los agentes; rutas privadas 🟠; datos personales 🟡; contenido no confiable 🟡), **M-2** MCPs como optimización (indexación de datos sensibles en FTS5/grafo — ya cubierto en `context-mode.md` y `ecosistema-documentacion.md`; MCPs no reemplazan la fuente de verdad 🟡), **M-3** actualización de memoria/índice (re-indexar propaga datos sensibles 🟠; índice desactualizado 🟡; `analisis-memoria.md` sin contenido sensible 🔵) y **M-4** acceso de todos los agentes (segmentar por rol 🟡; agentes con SSH sin credenciales en bitácoras 🟠; escritura restringida por rol 🟡). Incluye tabla de 14 riesgos→mitigación, recomendaciones de uso seguro, qué NO hacer y checklist. **Conclusión**: sin vulnerabilidades críticas que bloqueen la adopción, siempre que se respete la regla de **no escribir secrets en `Documentacion/`** (riesgo #1 🔴, mitigable con disciplina). Actualizado `pendientes-implementacion.md`.
  - **Fase implementación** (define el flujo concreto de actualización de herramientas):
    - `upgrade_framework`: Definir e implementar el flujo de actualización automática de herramientas externas (agregar/quitar/actualizar por carpeta).
    - `plataformador`: Implementar el flujo de contexto (consultar `Documentacion/` + validar MCPs + actualizar memoria/índice).
    - `gitflow`: Commits al final.
  - **Decisión del usuario (2026-09-12) — Re-indexación forzada + MCPs no instalados**: El usuario decidió que en la **primera consulta de cada sesión** se **fuerce la re-indexación** de los MCPs (`context-mode`, `codebase-memory-mcp`) y la actualización de `analisis-memoria.md` **antes** de que la IA consulte por MCP. La re-indexación es **procesamiento local sin IA** (FTS5/grafo, determinista y barato), por lo que no consume IA y garantiza índice/grafo **siempre fresco** → búsqueda por MCP más eficiente. Además, si un MCP **no está instalado** (no responde o no está en `.vscode/mcp.json`), se debe **instalarlo** (con confirmación del usuario) y **repetir el proceso de actualización de índices y grafos** antes de consultar. Flujo: **instalar → re-indexar → recién ahí consultar por MCP**. Esta decisión se incorporó al ADR-0002 (guardrails 4 y 5, diagrama Mermaid actualizado) y a las specs de `pensador` y `plataformador`.
  - **Prioridad**: alta

- [x] `[ECOSISTEMA]` **Ecosistema de documentación técnica sin IA de entrada (markitdown + graphify + codebase-memory-mcp + context-mode + analista_tecnico)**
  - **Qué implementar**: Pipeline de herramientas que generan documentación técnica sin IA de entrada (solo Python + MCP), IA solo bajo demanda, con archivo de memoria `analisis-memoria.md`. Nuevo agente `analista_tecnico` que orquesta el pipeline.
  - **Basado en**: `Documentacion/Agents_IA_TECH/README-ECOSISTEMA-DOCUMENTACION.md` (plan aprobado con 3 diagramas Mermaid reutilizables: pipeline, flujo del analista_tecnico, integración en arquitectura) + `Documentacion/Agents_IA_TECH/arquitectura/adr/adr-0001-ecosistema-documentacion-sin-ia.md` (ADR-0001: arquitectura del pipeline, guardrails y spec linking)
  - **Fase documental**:
    - `arquitecto`: ✅ **COMPLETADA (2026-09-12)** — ADR-0001 creado en `arquitectura/adr/adr-0001-ecosistema-documentacion-sin-ia.md` (contexto, decisión, consecuencias, guardrails, spec linking, plan por fases). Reutilizados diagramas **Pipeline** + **Integración en arquitectura**. Actualizados `00-indice.md` (ADRs activos) y `pendientes-implementacion.md`.
    - `documentador`: ✅ **AVANZADA (2026-09-12)** — Guías creadas en `MCPs/markitdown.md` (qué es, instalación `pip install 'markitdown[all]'` + `markitdown-mcp`, uso CLI `markitdown file.pdf -o file.md`, uso MCP `convert_to_markdown(uri)`, formatos soportados, integración en pipeline con diagrama **Pipeline** reutilizado, mantenimiento y buenas prácticas) y `MCPs/codebase-memory-mcp.md` (qué es, herramientas `index_repository`/`query`/`semantic_search`, instalación `npm install -g codebase-memory-mcp`, configuración, integración en pipeline con diagrama **Pipeline** reutilizado, uso por agentes documentales). Actualizados `referencias.md` (markitdown MIT, codebase-memory-mcp licencia a verificar), `memoria-proyecto.md` (ambos MCPs en integración), `roadmap.md` (integración del ecosistema), `00-indice.md` (estructura MCPs/ + tabla de MCPs), `pendientes-implementacion.md`.
    - `security-auditor`: ✅ **COMPLETADA (2026-09-12)** — Revisión de seguridad de uso creada en `Documentacion/Agents_IA_TECH/seguridad/ecosistema-documentacion.md` (análisis de riesgos de `markitdown` — XXE, ZIP bomb, HTML malicioso, PDF malformado — y de `codebase-memory-mcp` — indexación de secrets, datos sensibles, permisos del índice; tabla de riesgos→mitigación; recomendaciones de uso seguro; qué NO hacer; checklist; diagrama **Pipeline** reutilizado para señalar los puntos de entrada de datos PE-1 a PE-4). **Licencia de `codebase-memory-mcp` verificada: MIT** (guardrail 8 del ADR-0001 satisfecho; `npm view codebase-memory-mcp license` → MIT, repo https://github.com/DeusData/codebase-memory-mcp). Actualizados `referencias.md` (licencia MIT + URL), `00-indice.md` (tabla de MCPs).
  - **Fase implementación**:
    - `plataformador`: ✅ **COMPLETADA (2026-09-12)** — Instalado `markitdown` 0.1.7 (`pip install 'markitdown[all]'`, verificado `markitdown --version` → 0.1.7) y `markitdown-mcp` 0.0.1a3 (`pip install markitdown-mcp==0.0.1a3` + `pip install "mcp<2"` v1.30.0 por API FastMCP; verificado que arranca). `codebase-memory-mcp` 0.9.0 ya estaba instalado (`npm install -g codebase-memory-mcp`, verificado `codebase-memory-mcp --version` → 0.9.0). Creado `.vscode/mcp.json` con `markitdown` y `codebase-memory-mcp`. Hooks: no aplican a markitdown/codebase-memory-mcp (los hooks `.github/hooks/context-mode.json` son de context-mode, tarea `[MCP]` aparte). Actualizados `memoria-proyecto.md` (ambos MCPs 🟢 instalados) y `pendientes-implementacion.md`. **Nota**: `markitdown-mcp` de PyPI 0.0.1a1 es un stub sin servidor; la versión funcional es 0.0.1a3 (requiere `mcp<2`).
    - `upgrade_framework`: ✅ **COMPLETADA (2026-09-12)** — Registradas `markitdown` (0.1.7, MIT), `markitdown-mcp` (0.0.1a3, MIT) y `codebase-memory-mcp` (0.9.0, MIT) en `dependencias-manifest.yml` (fase implementación paso 5º). `graphify` marcada con licencia ⚠️ pendiente de verificar (guardrail 8 del ADR-0001). Actualizado historial del manifest.
    - `pensador` + documentales: ✅ **COMPLETADA (2026-09-12)** — Creada la spec del agente `analista_tecnico` en `agents/analista_tecnico/spec.md` (rol, responsabilidades, herramientas del pipeline, flujo con diagrama **Flujo del `analista_tecnico`** reutilizado, integración con diagrama **Integración en arquitectura** reutilizado, guardrails del ADR-0001, restricciones de paths, flujo típico, idioma, **regla dual-harness Copilot + OpenCode**). Creado el agente en **ambos harness**: `.github/agents/analista_tecnico.agent.md` (Copilot) y `.opencode/agents/analista_tecnico.md` (OpenCode). Creado `analisis-memoria.md` (archivo de memoria del pipeline). Actualizados `00-indice.md` (agente `analista_tecnico` 🟢 activo + estructura), `preferencias.md` (regla dual-harness) y `pendientes-implementacion.md`.
    - `gitflow`: Commits convencionales.
  - **Guardrails (ADR-0001)**: Sin IA de entrada por defecto (IA solo bajo demanda y preguntando al usuario); archivo de memoria `analisis-memoria.md` obligatorio (nunca re-analizar); orden de herramientas respetado; delegación no duplicación; retorno al `pensador`; respeto estructura SSD; reutilizar diagramas; verificar licencias; paths restringidos; validación de MCPs al iniciar sesión.
  - **Limpieza**: ✅ **DONE (2026-09-12)** — La carpeta vieja `Documentacion/Agents_IA_TECH/mcp/` (singular) fue **eliminada manualmente por el usuario**. Todas las referencias ya apuntan a `MCPs/` (plural). No quedan referencias a la carpeta vieja.
  - **Prioridad**: alta

- [x] `[ECOSISTEMA]` **Crear archivo de memoria `analisis-memoria.md` del pipeline**
  - **Qué implementar**: Crear `Documentacion/Agents_IA_TECH/analisis-memoria.md` que registre qué documentación fue convertida a MD (markitdown), indexada/graficada (graphify/codebase-memory-mcp), y qué requiere IA (y si ya fue analizada). Evita re-análisis.
  - **Basado en**: `Documentacion/Agents_IA_TECH/arquitectura/adr/adr-0001-ecosistema-documentacion-sin-ia.md` (sección "El archivo de memoria" + guardrail 2)
  - **Archivos esperados**: `Documentacion/Agents_IA_TECH/analisis-memoria.md`
  - **Estado**: ✅ **COMPLETADA (2026-09-12)** — Creado `Documentacion/Agents_IA_TECH/analisis-memoria.md` con estado del pipeline, registro detallado y cómo actualizarlo.
  - **Prioridad**: alta

- [x] `[ECOSISTEMA]` **Crear spec del agente `analista_tecnico`**
  - **Qué implementar**: Crear la spec del nuevo agente `analista_tecnico` (rol, responsabilidades, flujo, guardrails del ADR-0001, restricciones de paths) en `Documentacion/Agents_IA_TECH/agents/analista_tecnico/spec.md` y su `.agent.md` en `.github/agents/`. Reutilizar diagramas **Flujo del `analista_tecnico`** + **Integración en arquitectura**.
  - **Basado en**: `Documentacion/Agents_IA_TECH/arquitectura/adr/adr-0001-ecosistema-documentacion-sin-ia.md` (sección "El agente `analista_tecnico`" + guardrails) + `README-ECOSISTEMA-DOCUMENTACION.md`
  - **Archivos esperados**: `Documentacion/Agents_IA_TECH/agents/analista_tecnico/spec.md`, `.github/agents/analista_tecnico.agent.md`
  - **Estado**: ✅ **COMPLETADA (2026-09-12)** — Creada la spec en `agents/analista_tecnico/spec.md` y el `.agent.md` en `.github/agents/analista_tecnico.agent.md`. Diagramas **Flujo del `analista_tecnico`** + **Integración en arquitectura** reutilizados tal cual.
  - **Prioridad**: alta

- [x] `[MCP]` **Integrar context-mode como herramienta MCP del kit (instalación, configuración, uso y mantenimiento)**
  - **Qué implementar**: Integrar `context-mode` (https://github.com/mksglu/context-mode) como MCP listo para usar. Enfocado SOLO en instalación, configuración, utilización y mantenimiento de uso — NO desarrollo.
  - **Fase documental**:
    - `documentador`: ✅ **AVANZADA (2026-09-12)** — Guía práctica creada en `Documentacion/Agents_IA_TECH/MCPs/context-mode.md` (instalación, configuración en el kit, uso de herramientas `ctx_*`, mantenimiento: `ctx_purge` limpiar historial, `ctx_stats` ver stats, `ctx_upgrade` actualizar, `ctx_doctor` diagnosticar, opciones óptimas con menor uso de IA, seguridad). Actualizados `referencias.md` (licencia ELv2), `memoria-proyecto.md`, `roadmap.md`, `00-indice.md`, `pendientes-implementacion.md`.
    - `security-auditor`: ✅ **COMPLETADA (2026-09-12)** — Revisión breve de seguridad de uso creada en `Documentacion/Agents_IA_TECH/seguridad/context-mode.md` (ejecución sandbox, fetch de URLs/SSRF, datos indexados FTS5, licencia ELv2, hooks de VS Code, redacción de credenciales). Incluye tabla de riesgos→mitigación, recomendaciones de uso seguro, qué NO hacer y checklist.
  - **Fase implementación**:
    - `plataformador`: ✅ **COMPLETADA (2026-09-12)** — Registrado `context-mode` en `.vscode/mcp.json` (entrada `context-mode` con comando y descripción). Creados hooks `.github/hooks/context-mode.json` (PreToolUse, PostToolUse, SessionStart). Actualizado `memoria-proyecto.md` (context-mode 🟢 instalado + próximos pasos). **Nota**: la instalación real (`npm install -g context-mode`) requiere ejecutarse en terminal (Node >= 22.5) — pendiente de confirmación del usuario.
    - `upgrade_framework`: ✅ **COMPLETADA (2026-09-12)** — Registrado `context-mode` (v1.0.169, ELv2) en `dependencias-manifest.yml` (tipo MCP server, rol en pipeline, instalación, requisitos Node >= 22.5). Licencia ELv2 documentada (guardrail 8 del ADR-0001).
    - `pensador` + documentales: ✅ **COMPLETADA (2026-09-12)** — Ajustadas specs de `pensador` (responsabilidad 14: validar MCPs y documentación técnica), `plataformador` (sección "Validación de MCPs y documentación técnica" transparente), `arquitecto` (validar guías MCP + usar herramientas MCP) y `documentador` (documentar MCPs del ecosistema + usar herramientas MCP).
    - `gitflow`: Generar comandos de commit convencionales al final.
  - **Requisitos**: Node.js >= 22.5. Licencia ELv2 (source-available, no MIT).
  - **Prioridad**: alta
  - **Nota**: El routing de context-mode propone copiar su `copilot-instructions.md` encima del canónico del kit → FUSIONAR, no sobrescribir.

- [ ] `[MCP]` **Configurar agentes para usar codebase-memory-mcp**
  - **Qué implementar**: Ajustar instrucciones del `pensador`, `arquitecto` y `documentador` para que usen las herramientas MCP (index_repository, query, semantic_search, etc.)
  - **Estado actual**: 🟡 MCP `codebase-memory-mcp` en integración — guía creada en `Documentacion/Agents_IA_TECH/MCPs/codebase-memory-mcp.md` (2026-09-12). MCP aún no instalado (ver `Documentacion/Agents_IA_TECH/memoria-proyecto.md`)
  - **Siguiente paso**: Instalar MCP server (`npm install -g codebase-memory-mcp`) - tarea del `plataformador`
  - **Basado en**: `Documentacion/Agents_IA_TECH/MCPs/codebase-memory-mcp.md` + una vez instalado, ajustar specs en `agents/pensador/spec.md`, `agents/arquitecto/spec.md`, `agents/documentador/spec.md`
  - **Prioridad**: media
  - **Nota**: MCP habilita `index_repository`, `query`, `semantic_search` para búsquedas semánticas en el grafo de conocimiento. Los agentes documentales pueden usarlo para mejor contexturar specs y decisiones.

- [x] `[PERSISTENCIA]` **Sistema de persistencia de sesiones en disco**
  - **Qué implementar**: Pensador guarda análisis/planes/decisiones en `Documentacion/Agents_IA_TECH/sesiones/`. Al iniciar, lee la última sesión como base conceptual. Pregunta antes de borrar: "¿Querés guardar esta propuesta?"
  - **Infraestructura**: ✅ Completa - Folder `sesiones/` creado, sesión de ejemplo `2026-08-30-reestructuracion-completa.md`, regla en `preferencias.md`
  - **Basado en**: `Documentacion/Agents_IA_TECH/preferencias.md` (regla 2026-08-30)
  - **Archivos esperados**: `Documentacion/Agents_IA_TECH/sesiones/`, scripts de guardado/carga (convenio manual)
  - **Prioridad**: alta
  - **Nota**: La persistencia sigue el convenio de guardar archivos markdown en `sesiones/` con formato `YYYY-MM-DD-titulo.md`. Al iniciar VS Code, el Pensador lee la última sesión para recuperación de contexto.

- [x] `[SPECIFY]` **Definir integración explícita Pensador ↔ Specify (speckit skills)**
  - **Qué implementar**: Mapear qué skills de speckit invoca Pensador y en qué orden
  - **Integración definida**: ✅ Ya definida en `Documentacion/Agents_IA_TECH/referencias.md` con tabla complementariedad completa
  - **Orden de invocación Pensador → Specify**:
    1. `speckit-specify` → Generación de specs cuando el usuario solicita especificación
    2. `speckit-analyze` → Validación cross-artifact (spec ↔ plan ↔ tasks ↔ ADRs)
    3. `speckit-plan` → Generación de plan.md desde spec aprobada (orquesta Documentador)
    4. `speckit-tasks` → Generación de tasks.md desde plan aprobado (orquesta Documentador)
    5. `speckit-converge` → Verificación de implementación pendiente vs spec
    6. `speckit-implement` → Ejecución por parte de implementadores (api, frontend, devops)
    7. Feedback loop: QA-senior valida código implementado contra spec original
  - **Basado en**: `Documentacion/Agents_IA_TECH/referencias.md` (tabla complementariedad)
  - **Prioridad**: alta
  - **Notas**: Specify NO se duplica, los agentes complementan. Flujo integrado: Pensador decide → Specify ejecuta → Mis agentes complementan (SSH, plataformador, gitflow, orchestración) → Documentador persiste → Implementadores ejecutan → QA valida → Gitflow commitea

- [x] `[PLATAFORMADOR]` **Plataformador v2: Auditoría automática + reporte**
  - **Qué implementar**: Auditoría contra `.doc_agents/capacidad-base.md` con reporte detallado y auto-nivelación opcional (preguntar antes)
  - **Infraestructura**: ✅ El agente `plataformador` ya tiene capacidad de auditoría completa incluida en su spec (`agents/plataformador/spec.md`)
  - **Capacidades incluidas**:
    - Auditoría contra `.doc_agents/capacidad-base.md` (catálogo de kit)
    - Compara `Documentacion/Agents_IA_TECH/` contra `.doc_agents/estructura-estandar.md`
    - Detecta diferencias: archivos fuera de lugar, carpetas faltantes, huérfanos
    - Propone nivelación y pregunta antes de ejecutar
    - Orquesta `Arquitecto → Documentador → Security` para documentar proyecto
    - Integrates `graphify` para grafo de conocimiento
    - Actualiza `memoria-proyecto.md` con resultado
  - **Basado en**: `.doc_agents/capacidad-base.md`, `.doc_agents/estructura-aplicacion.md`, `.doc_agents/estructura-estandar.md`
  - **Prioridad**: alta
  - **Nota**: El agente plataformador puede ejecutarse manualmente o ser invocado por `pensador` al detectar proyecto nuevo/copiado o cambios en capacidad-base

---

## ✅ Tareas completadas

| Fecha | Tarea | Implementador |
|-------|-------|---------------|
| 2026-09-19 | **T-I1**: Actualizar `pensador.agent.md` y `pensador.md` con Constitution Check, Pipeline Speckit ordenado, Contexto Previo MCPs/Graphify, Delegación explícita, Skills Speckit (6) | `devops` |
| 2026-09-19 | **T-I9**: Actualizar `gitflow.agent.md` y `gitflow.md` — frontmatter `version: "2.0"`, skills `speckit-implement` + `speckit-converge`, sección Triggers (branch `feature/<task-id>`/PR + merge/tag semver/changelog). Verificado: Triggers idénticos en ambos harnesses, sección MCPs presente | `devops` |
| 2026-09-19 | **T-I10**: Actualizar `plataformador.agent.md` y `plataformador.md` — frontmatter `version: "2.0"`, skill `speckit-analyze`, sección Triggers (nivelación sync-agents/constitution drift + diagnóstico remoto health checks/codebase-memory). Verificado: Triggers idénticos en ambos harnesses, sección MCPs presente | `devops` |
| 2026-09-19 | **T-I11**: Actualizar `upgrade_framework.agent.md` y `upgrade_framework.md` (nombres reales con guion bajo; la spec T-I11 los cita con guion `upgrade-framework`) — frontmatter `version: "2.0"`, skills `speckit-plan` + `speckit-implement`, sección Triggers (detección framework obsoleto + plan migración `domain: upgrade`). Verificado: Triggers idénticos en ambos harnesses, sección MCPs presente | `devops` |
| 2026-09-19 | **T-I12**: Actualizar `analista_tecnico.agent.md` y `analista_tecnico.md` (nombres reales con guion bajo; la spec T-I12 los cita con guion `analista-tecnico`) — frontmatter `version: "2.0"`, skills `speckit-specify` + `speckit-analyze`, sección Triggers (investigación previa a specify + POCs/evaluación librerías con markitdown). Verificado: Triggers idénticos en ambos harnesses, sección MCPs presente | `devops` |
| 2026-09-19 | **T-I13**: Actualizar `solucionador.agent.md` y `solucionador.md` — frontmatter `version: "2.0"`, skills `speckit-analyze` + `speckit-implement`, sección Triggers (incidentes → RCA + hotfixes fast-track skip converge si crítico). Verificado: Triggers idénticos en ambos harnesses, sección MCPs presente | `devops` |
| 2026-09-19 | **T-I16**: Implementar Constitution Wizard (11 pasos interactivos + modo revisar/actualizar) en `pensador.agent.md` y `pensador.md` | `devops` |
| 2026-09-19 | **T-I14**: Actualizar `00-indice.md` con flujo unificado SSD+Speckit (mermaid Constitution Check → specify → implement con delegación) + tabla 13 agentes/skills/fase/trigger + rutas por app + enlaces `specs/metodologia-ssd-speckit/spec.md` y `MCPs/graphify.md`. Fecha → 2026-09-19 | `devops` |
| 2026-09-19 | **T-I17**: Actualizar `00-indice.md` con Constitution Wizard (tabla 11 pasos + enlace a RF-14 en `specs/metodologia-ssd-speckit/spec.md`) | `devops` |
| 2026-08-05 | Crear workflow de seguridad general (`security-scan.yml`) con gitleaks sobre todo el repo | `devops` |
| 2026-08-05 | Crear workflow de ortografía (`spellcheck.yml`) con codespell | `devops` |
| 2026-08-05 | Crear `.gitattributes` con normalización `eol=lf` | `devops` |
| 2026-08-05 | Actualizar `checkout`/`setup-node` a runtime node24 en `openwiki-update.yml` y `agentshield.yml` | `pensador` |
| 2026-07-27 | Reorganizar estructura `Documentacion/`: mover `adr/` → `arquitectura/adr/`, `specs/` → `funcionalidades/`, actualizar specs | `pensador` |
| 2026-08-30 | Mover documentación a `Documentacion/Agents_IA_TECH/`, crear análisis Specify vs Agentes, definir complementariedad | `pensador` |

## ⏳ Nuevas Tareas Críticas - Reglas de Oro

- [ ] `[REGLA-ORO]` **Ciclo Plan→Doc→Impl siempre vigente**
  - **Qué implementar**: Asegurar que todos los agentes respeten el orden sagrado: Plan aprobado → Documentar → Implementar (nunca saltar fases)
  - **Qué hacer**: Al detectar que un agente quiere saltar directamente a implementar o documentar, el Pensador debe: (1) Verificar en qué fase se encuentra realmente, (2) Si no es la correcta → regresar a la fase apropiada, (3) Nunca permitir implementar sin documentación completa y aprobada
  - **Prioridad**: alta
  - **Nota**: Esta es la regla principal para evitar el ciclo roto donde se salta documentación y se van directamente a ajustes sin documentar ni controlar memoria.

- [ ] `[ERROR-ROOT]` **Detección de causa raíz en debugging**
  - **Qué implementar**: El Pensador y todos los agentes deben buscar la causa real, no quedarse en círculos de ajustes superficiales. Cuando hay errores: (1) Analizar el error completo, (2) Identificar causa raíz, (3) Documentar el fix, (4) Aplicar fix, (5) Verificar. Si el error reaparece → regresar a causa raíz, no a ajustes parciales.
  - **Prioridad**: alta
  - **Nota**: Evita el patrón "solo ajusta y sigue" que rompe la documentación y memoria.

- [ ] `[CAMBIO-VISION]` **Reinicio automático al cambio de visión**
  - **Qué implementar**: Si el usuario cambia de visión en cualquier punto del proceso → REINICIAR el ciclo completo desde el análisis inicial
  - **Qué hacer**: En cualquier fase, si el usuario indica que quiere cambiar de dirección → el Pensador debe regresar al Paso 2 (ANÁLISIS PRIMERO) y comenzar de nuevo
  - **Prioridad**: alta
  - **Nota**: Evita que se queden en trabajo inútil cuando el usuario ha cambiado de opinión.
  - **Fase documental**:
    - `documentador`: ✅ **COMPLETADA (2026-09-19)** — Spec completa en `specs/cambio-vision/spec.md` (v1.0: Objetivo + RF-01..RF-05 por disparador + RF-06 procedimiento común con preserva/descarte + pregunta "¿Querés guardar esta propuesta?" + 7 ACs + contador de iteraciones por operación + ajuste menor vs cambio de visión + compatibilidad con regla vigente del `pensador` sin contradicción). Disparadores formalizados sin cambios: 1) resultado mal → PARAR + Paso 2, 2) 3ª iteración criterio cambiante → PREGUNTAR specs, 3) mejor forma → inicio diseño + impacto, 4) cambio requerimiento → `security-auditor` re-valida, 5) explícito → confirmar antes de reiniciar.
    - `security-auditor`: ✅ **COMPLETADA (2026-09-19)** — Revisión de seguridad creada en `seguridad/cambio-vision.md` (formato §§1-7 heredado; 6 riesgos: mitigaciones invalidadas en silencio 🔴, descarte borra el "por qué no" 🟠, ajuste-menor-vs-visión mal clasificado 🟠, sesión archivada con secrets 🟠, contador evadido 🟡, reinicio sin traza 🟡). **Conclusión: no bloquea, con condiciones** (reporte AC-04 con matriz mitigación↔supuesto como pre-condición del Paso 2, portar "rechazado por riesgo", fail-closed en la frontera + reclasificación, barrido de secrets pre-archivo, contador auditable + traza primero; `qa-senior` valida 7 ACs con fixtures de los 6 riesgos).
    - `qa-senior`: pendiente — validar 7 ACs contra la spec.
  - **Fase implementación**:
    - `devops`: ✅ **COMPLETADA (2026-09-19)** — Sección "Cambio de visión" agregada en ambas definiciones del `pensador` (`.github/agents/pensador.agent.md` y `.opencode/agents/pensador.md`, texto idéntico): 5 disparadores (1-está mal→PARAR+RF-06, 2-contador 1ª/2ª normal + 3ª PREGUNTAR specs, 3-mejor forma→diseño+impacto, 4-requerimiento→re-validación `security-auditor` con reporte AC-04 matriz mitigación↔supuesto como pre-condición del Paso 2, 5-explícito→confirmación) + frontera ajuste menor vs visión con fail-closed (AC-07) + RF-06 (Constitution Check ligero→Paso 2, preserva sesión+transversales con barrido de secrets, descarta plan/specs viejas portando "rechazado por riesgo", pregunta "¿Querés guardar esta propuesta?", traza antes del Paso 2). Extiende "Replanificar" sin contradicción. Sync `.github/` ↔ `.opencode/` verificado (sección idéntica).

- [ ] `[UPGRADE_FRAMEWORK]` **Implementar agente de actualización inteligente de framework**
  - **Qué implementar**: Crear el agente `upgrade_framework` que gestiona directorios de proyectos externos, sabe qué copiar dónde, aplica configuración necesaria y personalizaciones inteligentes, usando IA solo para análisis de impacto
  - **Basado en**: `Documentacion/Agents_IA_TECH/agents/upgrade_framework/spec.md`
  - **Archivos esperados**: Estructura completa en `Documentacion/Agents_IA_TECH/agents/upgrade_framework/`
  - **Prioridad**: alta

- [ ] `[SSD-SPECKIT-INTEGRACION]` **Integrar uso diario transparente SSD + Speckit + Graphify + MCPs en flujo de todos los agentes**
  - **Qué implementar**: Constitution FIRST → Speckit pipeline (specify→plan→tasks→analyze→converge→implement) → Agentes complementan según su ámbito. Pensador orquesta pipeline completo; Arquitecto/Documentador/Security complementan fase documental; Implementadores complementan fase implementación; Transversales en su momento. MCPs + Graphify como contexto previo ANTES de cada llamada speckit-*. Rutas guardado por app: `src/<app>/` + `Documentacion/<app>/specs/`.
  - **Fase documental**:
    - `documentador`: ✅ **COMPLETADA (2026-09-19)** — Spec completa en `specs/metodologia-ssd-speckit/` (spec.md: 13 RFs + 10 ACs, plan.md, tasks.md) cubriendo Constitution check, Speckit pipeline ordenado, TODOS los 13 agentes con sus skills, MCPs/Graphify como contexto previo, rutas por app.
    - `security-auditor`: ✅ **COMPLETADA (2026-09-19)** — Revisión de seguridad creada en `seguridad/metodologia-ssd.md` (formato §§1-7 heredado; 10 riesgos: gates Constitution no válidos 🔴, specs desactualizadas 🔴, drift Constitution 🟠, Speckit sin contexto 🟠, agentes huérfanos 🟠, rutas por app desincronizadas 🟠, supply chain Speckit 🟠, Graphify MCP expuesto 🟠, secrets en grafo 🟠, Tokenslayer apply_patch sin auditoría 🟠). **Conclusión: no bloquea la adopción, con condiciones** (Constitution check por fase + hash validation, drift detection automatizado con reinicio de ciclo, pre-condición de contexto obligatoria en wrappers Speckit, runtime skill enforcement con hook de auditoría, AppName inyectado y validado en artefactos, Spec-kit pinned en manifest + hash verificado, Graphify MCP stdio-only enforceado, exclusión de secrets en grafo + prompt defense, trazabilidad completa apply_patch, post-implementation drift check en converge; `qa-senior` valida con fixtures cubriendo los 10 riesgos).
  - **Fase implementación**:
    - `devops`: 1) Agregar speckit skills a TODOS los `.agent.md` (Pensador primero, luego cada agente según su fase) + mapeo explícito cuándo invocar cada speckit-*. 2) Paso "**Contexto Previo**" en Pensador (nombre real en `pensador.agent.md`; citado en la spec como "Conectar a elementos") — MCPs + Graphify + codebase-memory antes de cada speckit-*). 3) Actualizar `00-indice.md` con flujo unificado + tabla agentes/skills/speckit + rutas por app. 4) Crear `MCPs/graphify.md` (guía práctica: instalado, MCP stdio, 10 tools, cómo consultar grafo arquitectura).
    - `qa-senior`: Tests: Constitution check PASS/FAIL, speckit pipeline ejecutable end-to-end en fixture, agentes complementan sobre artefactos speckit, MCPs/Graphify enriquecen contexto.
  - **Fase validación**:
    - `qa-senior`: ✅ **RE-VALIDACIÓN 2026-09-19 — 8/8 PASS, T-V1 → PASS (BUG-1 CERRADO)**. Criterio vigente "**6 artículos (I–VI, con VI=proyect_ext)**" = constitución real (I L6, II L9, III L12, IV L15, V L18, VI proyect_ext L21); cero "9 artículos" en spec/plan/tasks/00-indice/seguridad-metodologia (`Select-String` 0 matches); scripts `create-new-feature/setup-plan/setup-tasks` presentes en `.specify/scripts/powershell/`. T-V2..T-V8 sin cambios (PASS). OBS menor nueva no bloqueante para `documentador`: `tasks.md` T-V8 paso 2 (L312) cita "Art.VII" inexistente con 6 artículos. BUG-2..BUG-6 siguen abiertos como no bloqueantes. **Conclusión: GO con observaciones** (condiciones de `seguridad/metodologia-ssd.md` como requisitos de implementación). Reporte: `Documentacion/Agents_IA_TECH/testing/validacion-ssd-speckit-2026-09-19.md`.
   - **Basado en**: Plan aprobado 2026-09-19 (Constitution FIRST, Speckit FIRST, agentes complementan, MCPs/Graphify contexto previo, todos los 13 agentes integrados sin huérfanos).
   - **Corrección `documentador` (2026-09-19)**: BUG-1..5 corregidos por `documentador` 2026-09-19, pendiente re-validación T-V1. Detalle: BUG-1 → criterio corregido a **6 artículos (I–VI, con VI=proyect_ext)** en `specs/metodologia-ssd-speckit/spec.md` (Objetivo, RF-01, RF-14, AC-11) y `00-indice.md` (mermaid + tabla Wizard paso 9); BUG-2 → NOTA al pie en `00-indice.md` (agentes manuales/legacy fuera del pipeline); BUG-3 → `upgrade_framework` / `analista_tecnico` con guion bajo en `plan.md` (T-D8c/T-D8d) y `spec.md` (RF-08) — `tasks.md` T-I11/T-I12 pendiente (fuera de paths de este rol); BUG-4 → paso aclarado como "**Contexto Previo**" (nombre real en `pensador.agent.md`); BUG-5 → tabla explícita RF↔task↔agente (14 RFs) en `spec.md`. ✅ **Re-validado por `qa-senior` 2026-09-19 (T-V1 PASS).**
      - `security-auditor`: ✅ **LIMPIEZA FINAL BUG-1 (2026-09-19)** — Corregido criterio erróneo "9 artículos" / "9 + Art.VI" remanente en `seguridad/metodologia-ssd.md` (L14: `Art.VII/VIII/IX` + "6 base + Art.VI" → **6 artículos (I–VI, con VI=proyect_ext)**; L154: Referencias → **6 artículos (I–VI, con VI=proyect_ext)**). Verificado: cero ocurrencias restantes de "9 artículos" en el archivo. Severidad sin cambios (R-1 🔴 y R-3 🟠 intactos — el riesgo de drift sigue válido: 6 vs 6, no 6 vs 9).
      - `qa-senior`: ✅ **RE-VALIDACIÓN T-V1 COMPLETADA (2026-09-19)** — T-V1 → PASS, BUG-1 CERRADO, 8/8 PASS, GO con observaciones (detalle en entrada `qa-senior` de Fase validación).
   - **Prioridad**: alta

- [x] `[UPGRADE_EXECUTADO]` **Ejecutar actualización de herramientas externas vía upgrade_framework**
  - **Qué se ejecutó**: Agente `upgrade_framework` para actualizar herramientas externas listadas en dependencias-manifest.yml
  - **Basado en**: `Documentacion/Agents_IA_TECH/agents/upgrade_framework/spec.md`
  - **Resultado**: Actualización completada de spec-kit, graphify y otras dependencias listadas
  - **Fecha**: 2026-09-05
  - **Nota**: Se usó IA solo para análisis de impacto de integración, siguiendo las reglas de uso eficiente de IA